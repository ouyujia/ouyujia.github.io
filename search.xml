<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>C++新特性--智能指针</title>
      <link href="/2022/04/22/zhi-neng-zhi-zhen/"/>
      <url>/2022/04/22/zhi-neng-zhi-zhen/</url>
      
        <content type="html"><![CDATA[<h2 id="智能指针类型"><a href="#智能指针类型" class="headerlink" title="智能指针类型"></a>智能指针类型</h2><p>在 C++ 中没有垃圾回收机制，必须自己释放分配的内存，否则就会造成内存泄露。解决这个问题最有效的方法是使用智能指针（smart pointer）。<font color="orange"><code>智能指针是存储指向动态分配（堆）对象指针的类，用于生存期的控制，能够确保在离开指针所在作用域时，自动地销毁动态分配的对象，防止内存泄露。智能指针的核心实现技术是引用计数，每使用它一次，内部引用计数加1，每析构一次内部的引用计数减1，减为0时，删除所指向的堆内存。</code></font></p><p>C++11 中提供了三种智能指针，使用这些智能指针时需要引用头文件<font color="orange"><code>&lt;memory&gt;</code></font>:</p><p><font color="orange"><code>std::shared_ptr</code></font>：共享的智能指针<br><font color="orange"><code>std::unique_ptr</code></font>：独占的智能指针<br><font color="orange"><code>std::weak_ptr</code></font>：弱引用的智能指针，它不共享指针，不能操作资源，是用来监视 <font color="orange"><code>shared_ptr</code> </font>的。</p><h3 id="共享智能指针–shared-ptr"><a href="#共享智能指针–shared-ptr" class="headerlink" title="共享智能指针–shared_ptr"></a>共享智能指针–shared_ptr</h3><h4 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h4><p>共享智能指针是指多个智能指针可以同时管理同一块有效的内存，共享智能指针<font color="orange"> shared_ptr</font> 是一个模板类，如果要进行初始化有三种方式：<font color="orange">通过构造函数</font>、<font color="orange">std::make_shared 辅助函数</font>以及<font color="orange"> reset </font>方法。共享智能指针对象初始化完毕之后就指向了要管理的那块堆内存，如果想要查看当前有多少个智能指针同时管理着这块内存可以使用共享智能指针提供的一个成员函数 <font color="orange">use_count</font>，函数原型如下：</p><pre class="line-numbers language-C++"><code class="language-C++">// 管理当前对象的 shared_ptr 实例数量，或若无被管理对象则为 0。long use_count() const noexcept;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><h5 id="通过构造函数初始化"><a href="#通过构造函数初始化" class="headerlink" title="通过构造函数初始化"></a>通过构造函数初始化</h5><pre class="line-numbers language-C++"><code class="language-C++">// shared_ptr<T> 类模板中，提供了多种实用的构造函数, 语法格式如下:std::shared_ptr<T> 智能指针名字(创建堆内存);<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>测试代码如下:</p><pre class="line-numbers language-C++"><code class="language-C++">#include <iostream>#include <memory>using namespace std;int main(){    // 使用智能指针管理一块 int 型的堆内存    shared_ptr<int> ptr1(new int(520));    cout << "ptr1管理的内存引用计数: " << ptr1.use_count() << endl;    // 使用智能指针管理一块字符数组对应的堆内存    shared_ptr<char> ptr2(new char[12]);    cout << "ptr2管理的内存引用计数: " << ptr2.use_count() << endl;    // 创建智能指针对象, 不管理任何内存    shared_ptr<int> ptr3;    cout << "ptr3管理的内存引用计数: " << ptr3.use_count() << endl;    // 创建智能指针对象, 初始化为空    shared_ptr<int> ptr4(nullptr);    cout << "ptr4管理的内存引用计数: " << ptr4.use_count() << endl;    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>测试代码输出的结果如下:</p><pre class="line-numbers language-C++"><code class="language-C++">ptr1管理的内存引用计数: 1ptr2管理的内存引用计数: 1ptr3管理的内存引用计数: 0ptr4管理的内存引用计数: 0<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p><font color="red">如果智能指针被初始化了一块有效内存，那么这块内存的引用计数 + 1，如果智能指针没有被初始化或者被初始化为 nullptr 空指针，引用计数不会 + 1。另外，不要使用一个原始指针初始化多个 shared_ptr。</font></p></blockquote><pre class="line-numbers language-C++"><code class="language-C++">int *p = new int;shared_ptr<int> p1(p);shared_ptr<int> p2(p);// error, 编译不会报错, 运行会出错<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><h5 id="通过拷贝和移动构造函数初始化"><a href="#通过拷贝和移动构造函数初始化" class="headerlink" title="通过拷贝和移动构造函数初始化"></a>通过拷贝和移动构造函数初始化</h5><p>当一个智能指针被初始化之后，就可以通过这个智能指针初始化其他新对象。在创建新对象的时候，对应的拷贝构造函数或者移动构造函数就被自动调用了。</p><pre class="line-numbers language-C++"><code class="language-C++">#include <iostream>#include <memory>using namespace std;int main(){    // 使用智能指针管理一块 int 型的堆内存, 内部引用计数为 1    shared_ptr<int> ptr1(new int(520));    cout << "ptr1管理的内存引用计数: " << ptr1.use_count() << endl;    //调用拷贝构造函数    shared_ptr<int> ptr2(ptr1);    cout << "ptr2管理的内存引用计数: " << ptr2.use_count() << endl;    shared_ptr<int> ptr3 = ptr1;    cout << "ptr3管理的内存引用计数: " << ptr3.use_count() << endl;    //调用移动构造函数    shared_ptr<int> ptr4(std::move(ptr1));    cout << "ptr4管理的内存引用计数: " << ptr4.use_count() << endl;    std::shared_ptr<int> ptr5 = std::move(ptr2);    cout << "ptr5管理的内存引用计数: " << ptr5.use_count() << endl;    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>测试程序输入的结果:</p><pre class="line-numbers language-C++"><code class="language-C++">ptr1管理的内存引用计数: 1ptr2管理的内存引用计数: 2ptr3管理的内存引用计数: 3ptr4管理的内存引用计数: 3ptr5管理的内存引用计数: 3<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><h5 id="通过-std-make-shared初始化"><a href="#通过-std-make-shared初始化" class="headerlink" title="通过 std::make_shared初始化"></a>通过 std::make_shared初始化</h5><p>通过 C++ 提供的<font color="orange"> <code>std::make_shared()</code> </font>就可以完成内存对象的创建并将其初始化给智能指针，函数原型如下：</p><pre class="line-numbers language-C++"><code class="language-C++">template< class T, class... Args >shared_ptr<T> make_shared( Args&&... args );<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><ul><li><font color="orange">T</font>：模板参数的数据类型</li><li><font color="orange">Args&amp;&amp;… args </font>：要初始化的数据，如果是通过 make_shared 创建对象，需按照构造函数的参数列表指定</li></ul><p>测试代码如下:</p><pre class="line-numbers language-C++"><code class="language-C++">#include <iostream>#include <string>#include <memory>using namespace std;class Test{public:    Test()     {        cout << "construct Test..." << endl;    }    Test(int x)     {        cout << "construct Test, x = " << x << endl;    }    Test(string str)     {        cout << "construct Test, str = " << str << endl;    }    ~Test()    {        cout << "destruct Test ..." << endl;    }};int main(){    // 使用智能指针管理一块 int 型的堆内存, 内部引用计数为 1    shared_ptr<int> ptr1 = make_shared<int>(520);    cout << "ptr1管理的内存引用计数: " << ptr1.use_count() << endl;    shared_ptr<Test> ptr2 = make_shared<Test>();    cout << "ptr2管理的内存引用计数: " << ptr2.use_count() << endl;    shared_ptr<Test> ptr3 = make_shared<Test>(520);    cout << "ptr3管理的内存引用计数: " << ptr3.use_count() << endl;    shared_ptr<Test> ptr4 = make_shared<Test>("我是要成为海贼王的男人!!!");    cout << "ptr4管理的内存引用计数: " << ptr4.use_count() << endl;    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p><font color="red">使用 std::make_shared() 模板函数可以完成内存地址的创建，并将最终得到的内存地址传递给共享智能指针对象管理。如果申请的内存是普通类型，通过函数的（）可完成地址的初始化，如果要创建一个类对象，函数的（）内部需要指定构造对象需要的参数，也就是类构造函数的参数。</font></p></blockquote><h5 id="通过-reset-方法初始化"><a href="#通过-reset-方法初始化" class="headerlink" title="通过 reset 方法初始化"></a>通过 reset 方法初始化</h5><p><font color="orange"> <code>std::shared_ptr::reset</code></font> 有两种功能：</p><ul><li><font color="orange">通过 reset 方法来初始化</font></li><li><font color="orange">调用 reset 会将共享智能指针引用计数减 1。</font></li></ul><p>共享智能指针类提供的<font color="orange"> <code>std::shared_ptr::reset</code></font> 方法函数原型如下：</p><pre class="line-numbers language-C++"><code class="language-C++">void reset() noexcept;template< class Y >void reset( Y* ptr );template< class Y, class Deleter >void reset( Y* ptr, Deleter d );template< class Y, class Deleter, class Alloc >void reset( Y* ptr, Deleter d, Alloc alloc );<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li><font color="orange">ptr</font>：指向要取得所有权的对象的指针</li><li><font color="orange">d</font>：指向要取得所有权的对象的指针</li><li><font color="orange">aloc</font>：内部存储所用的分配器aloc</li></ul><p>测试代码如下：</p><pre class="line-numbers language-C++"><code class="language-C++">#include <iostream>#include <string>#include <memory>using namespace std;int main(){    // 使用智能指针管理一块 int 型的堆内存, 内部引用计数为 1    shared_ptr<int> ptr1 = make_shared<int>(520);    shared_ptr<int> ptr2 = ptr1;    shared_ptr<int> ptr3 = ptr1;    shared_ptr<int> ptr4 = ptr1;    cout << "ptr1管理的内存引用计数: " << ptr1.use_count() << endl;    cout << "ptr2管理的内存引用计数: " << ptr2.use_count() << endl;    cout << "ptr3管理的内存引用计数: " << ptr3.use_count() << endl;    cout << "ptr4管理的内存引用计数: " << ptr4.use_count() << endl;    ptr4.reset();    cout << "ptr1管理的内存引用计数: " << ptr1.use_count() << endl;    cout << "ptr2管理的内存引用计数: " << ptr2.use_count() << endl;    cout << "ptr3管理的内存引用计数: " << ptr3.use_count() << endl;    cout << "ptr4管理的内存引用计数: " << ptr4.use_count() << endl;    shared_ptr<int> ptr5;    ptr5.reset(new int(250));    cout << "ptr5管理的内存引用计数: " << ptr5.use_count() << endl;    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      <categories>
          
          <category> C++新特性 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> C++11新特性 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>C++11新特性--右值引用</title>
      <link href="/2022/04/21/you-zhi-yin-yong/"/>
      <url>/2022/04/21/you-zhi-yin-yong/</url>
      
        <content type="html"><![CDATA[<h3 id="右值引用"><a href="#右值引用" class="headerlink" title="右值引用"></a>右值引用</h3><h4 id="右值"><a href="#右值" class="headerlink" title="右值"></a>右值</h4><p>C++11 增加了一个新的类型，称为<font color="orange">右值引用</font>（ R-value reference），标记为 <font color="orange">&amp;&amp;</font>。在介绍<font color="orange">右值引用</font>类型之前先要了解什么是<font color="orange">左值</font>和<font color="orange">右值</font>：</p><ul><li><font color="orange">lvalue </font>是 <font color="cornflowerblue">loactor value</font> 的缩写，<font color="orange">rvalue </font>是 <font color="orange">read value</font> 的缩写</li><li><font color="orange">左值</font>是<font color="red">指存储在内存中、有明确存储地址（可取地址）的数据；</font></li><li><font color="orange">右值</font>是<font color="red">指可以提供数据值的数据（不可取地址）</font></li></ul><p>通过描述可以看出，区分左值与右值的便捷方法是：<font color="red">可以对表达式取地址（&amp;）就是左值，否则为右值 </font>。<font color="orange">所有有名字的变量或对象都是左值，而右值是匿名的。</font></p><pre class="line-numbers language-C++"><code class="language-C++">int a = 520;int b = 1314;a = b;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>一般情况下，位于<font color="orange"> =</font> 前的表达式为左值，位于 <font color="orange">= </font>后边的表达式为右值。也就是说例子中的<font color="orange"> a, b</font> 为左值，<font color="orange">520,1314 </font>为右值。<font color="orange">a=b</font> 是一种特殊情况，在这个表达式中<font color="orange"> a, b </font>都是左值，因为变量<font color="orange"> b</font> 是可以被取地址的，不能视为右值。</p><hr><p>C++11 中右值可以分为两种：一个是<font color="orange">将亡值</font>（ xvalue, expiring value），另一个则是<font color="orange">纯右值</font>（ prvalue, PureRvalue）：</p><p><font color="orange">纯右值</font>：<font color="red">非引用返回的临时变量、运算表达式产生的临时变量、原始字面量和 lambda 表达式等</font><br><font color="orange">将亡值</font>：<font color="red">与右值引用相关的表达式，比如，T&amp;&amp; 类型函数的返回值、 std::move 的返回值等。</font></p><hr><h4 id="右值引用-1"><a href="#右值引用-1" class="headerlink" title="右值引用"></a>右值引用</h4><p>右值引用就是对一个右值进行引用的类型。因为右值是匿名的，所以我们只能通过引用的方式找到它。<font color="orange">无论声明左值引用还是右值引用都必须立即进行初始化，因为引用类型本身并不拥有所绑定对象的内存，只是该对象的一个别名。通过右值引用的声明，该右值又“重获新生”</font>，<font color="red">其生命周期与右值引用类型变量的生命周期一样，只要该右值引用类型变量还活着，该右值临时量将会一直存活下去。</font></p><p>关于右值引用的使用，参考代码如下：</p><pre class="line-numbers language-C++"><code class="language-C++">#include <iostream>using namespace std;int&& value = 520;class Test{public:    Test()    {        cout << "construct: my name is jerry" << endl;    }    Test(const Test& a)    {        cout << "copy construct: my name is tom" << endl;    }};Test getObj(){    return Test();}int main(){    int a1;    int &&a2 = a1;        // error    Test& t = getObj();   // error    Test && t = getObj();    const Test& t = getObj();    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li>在上面的例子中<font color="orange"> int&amp;&amp; value = 520;</font> 里面<font color="orange"> 520 </font>是纯右值，<font color="orange">value </font>是对字面量 <font color="orange">520 </font>这个右值的引用。</li><li>在 <font color="orange">int &amp;&amp;a2 = a1; </font>中 <font color="orange">a1</font> 虽然写在了 <font color="orange">= </font>右边，但是它仍然是一个左值，<font color="orange">使用左值初始化一个右值引用类型是不合法的。</font></li><li>在 <font color="orange">Test&amp; t = getObj()</font> 这句代码中语法是错误的，<font color="orange">右值不能给普通的左值引用赋值。</font></li><li>在 <font color="orange">Test &amp;&amp; t = getObj();</font> 中 <font color="orange">getObj() </font>返回的<font color="orange">临时对象</font>被称之为<font color="orange">将亡值</font>，<font color="orange">t </font>是这个<font color="orange">将亡值</font>的右值引用。</li><li><font color="orange">const Test&amp; t = getObj() </font>这句代码的语法是正确的，<font color="orange">常量左值引用是一个万能引用类型，它可以接受左值、右值、常量左值和常量右值。</font></li></ul><h3 id="性能优化"><a href="#性能优化" class="headerlink" title="性能优化"></a>性能优化</h3><p>在 C++ 中在进行对象赋值操作的时候，很多情况下会发生对象之间的深拷贝，如果堆内存很大，这个拷贝的代价也就非常大，在某些情况下，如果想要<font color="orange">避免对象的深拷贝，就可以使用右值引用进行性能的优化</font>。</p><p>再来修改一下上面的实例代码：</p><pre class="line-numbers language-C++"><code class="language-C++">#include <iostream>using namespace std;class Test{public:    Test() : m_num(new int(100))    {        cout << "construct: my name is jerry" << endl;    }    Test(const Test& a) : m_num(new int(*a.m_num))    {        cout << "copy construct: my name is tom" << endl;    }    ~Test()    {        delete m_num;    }    int* m_num;};Test getObj(){    Test t;    return t;}int main(){    Test t = getObj();    cout << "t.m_num: " << *t.m_num << endl;    return 0;};<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>测试代码执行的结果为:</p><pre class="line-numbers language-C++"><code class="language-C++">construct: my name is jerrycopy construct: my name is tomt.m_num: 100<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>通过输出的结果可以看到调用 <font color="orange">Test t = getObj();</font> 的时候调用拷贝构造函数对返回的临时对象进行了深拷贝得到了对象 <font color="orange">t</font>，在<font color="orange"> getObj()</font> 函数中创建的对象虽然进行了内存的申请操作，但是没有使用就释放掉了。如果能够使用临时对象已经申请的资源，既能节省资源，还能节省资源申请和释放的时间，如果要执行这样的操作就需要使用右值引用了，<font color="red">右值引用具有移动语义，移动语义可以将资源（堆、系统对象等）通过浅拷贝从一个对象转移到另一个对象这样就能减少不必要的临时对象的创建、拷贝以及销毁，可以大幅提高 C++ 应用程序的性能。</font></p><hr><pre class="line-numbers language-C++"><code class="language-C++">#include <iostream>using namespace std;class Test{public:    Test() : m_num(new int(100))    {        cout << "construct: my name is jerry" << endl;    }    Test(const Test& a) : m_num(new int(*a.m_num))    {        cout << "copy construct: my name is tom" << endl;    }    // 添加移动构造函数    Test(Test&& a) : m_num(a.m_num)    {        a.m_num = nullptr;        cout << "move construct: my name is sunny" << endl;    }    ~Test()    {        delete m_num;        cout << "destruct Test class ..." << endl;    }    int* m_num;};Test getObj(){    Test t;    return t;}int main(){    Test t = getObj();    cout << "t.m_num: " << *t.m_num << endl;    return 0;};<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>测试代码执行的结果如下:</p><pre class="line-numbers language-C++"><code class="language-C++">construct: my name is jerrymove construct: my name is sunnydestruct Test class ...t.m_num: 100destruct Test class ...<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>通过修改，在上面的代码给 <font color="orange">Test </font>类添加了<font color="orange">移动构造函数（参数为右值引用类型）</font>，这样在进行 <font color="orange">Test t = getObj(); </font>操作的时候并没有调用拷贝构造函数进行深拷贝，而是调用了移动构造函数，在这个函数中只是进行了浅拷贝，没有对临时对象进行深拷贝，提高了性能。</p><p>如果不使用移动构造，<font color="orange"><del>也没有提供深拷贝构造函数</del></font>，在执行<font color="orange"> Test t = getObj() </font>的时候也是进行了浅拷贝，但是当临时对象被析构的时候，类成员指针 <font color="orange">int* m_num;</font> 指向的内存也就被析构了，对象 t 也就无法访问这块内存地址了。</p><p>在测试程序中 <font color="orange">getObj() </font>的返回值就是一个将亡值，也就是说是一个右值，在进行赋值操作的时候如果 <font color="orange">= </font>右边是一个右值，那么移动构造函数就会被调用。<font color="orange">移动构造中使用了右值引用，会将临时对象中的堆内存地址的所有权转移给对象t，这块内存被成功续命，因此在t对象中还可以继续使用这块内存。</font></p><hr><blockquote><p><font color="red">对于需要动态申请大量资源的类，应该设计移动构造函数，以提高程序效率。需要注意的是，我们一般在提供移动构造函数的同时，也会提供常量左值引用的拷贝构造函数，以保证移动不成还可以使用拷贝构造函数</font><font color="red">。</font></p></blockquote><h3 id="amp-amp-的特性"><a href="#amp-amp-的特性" class="headerlink" title="&amp;&amp;的特性"></a>&amp;&amp;的特性</h3><p>在 C++ 中，并不是所有情况下 <font color="orange">&amp;&amp;</font> 都代表是一个右值引用，具体的场景体现在模板和自动类型推导中，如果是模板参数需要指定为<font color="orange"> T&amp;&amp;</font>，如果是自动类型推导需要指定为 <font color="orange">auto &amp;&amp;</font>，在这两种场景下 <font color="orange">&amp;&amp; </font>被称作<font color="orange">未定的引用类型</font>。另外还有一点需要额外注意 <font color="orange">const T&amp;&amp;</font> 表示一个右值引用，不是未定引用类型。</p><p>先来看第一个例子，在函数模板中使用<font color="orange"> &amp;&amp;</font>:</p><pre class="line-numbers language-C++"><code class="language-C++">template<typename T>void f(T&& param);void f1(const T&& param);f(10); int x = 10;f(x); f1(x);<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>在上面的例子中函数模板进行了自动类型推导，需要通过传入的实参来确定参数 param 的实际类型。</p><ul><li>第 4 行中，对于<font color="orange"> f(10) </font>来说传入的实参 10 是右值，因此 <font color="orange">T&amp;&amp;</font> 表示右值引用</li><li>第 6 行中，对于 <font color="orange">f(x)</font> 来说传入的实参是 x 是左值，因此 <font color="orange">T&amp;&amp; </font>表示左值引用</li><li>第 7 行中，<font color="orange">f1(x) </font>的参数是 <font color="orange">const T&amp;&amp;</font> 不是未定引用类型，不需要推导，本身就表示一个右值引用</li></ul><p>再来看第二个例子:</p><pre class="line-numbers language-C++"><code class="language-C++">int main(){    int x = 520, y = 1314;    auto&& v1 = x;    auto&& v2 = 250;    decltype(x)&& v3 = y;   // error    cout << "v1: " << v1 << ", v2: " << v2 << endl;    return 0;};<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li>第 4 行中 <font color="orange">auto&amp;&amp; </font>表示一个整形的左值引用</li><li>第 5 行中 <font color="orange">auto&amp;&amp;</font> 表示一个整形的右值引用</li><li>第 6 行中 <font color="orange">decltype(x)&amp;&amp;</font> 等价于 <font color="orange">int&amp;</font>&amp; 是一个右值引用不是未定引用类型，y 是一个左值，<font color="orange">不能使用左值初始化一个右值引用类型。</font></li></ul><p>由于上述代码中存在 <font color="orange">T&amp;&amp; </font>或者 <font color="orange">auto&amp;&amp;</font> 这种未定引用类型，当它作为参数时，有可能被一个右值引用初始化，也有可能被一个左值引用初始化，在进行类型推导时右值引用类型（&amp;&amp;）会发生变化，这种变化被称为<font color="orange">引用折叠</font>。在 C++11 中引用折叠的规则如下：</p><ul><li><font color="red">通过右值推导 T&amp;&amp; 或者 auto&amp;&amp; 得到的是一个右值引用类型</font></li><li><font color="red">通过非右值（右值引用、左值、左值引用、常量右值引用、常量左值引用）推导 T&amp;&amp; 或者 auto&amp;&amp; 得到的是一个左值引用类型</font></li></ul><pre class="line-numbers language-C++"><code class="language-C++">int&& a1 = 5;auto&& bb = a1;auto&& bb1 = 5;int a2 = 5;int &a3 = a2;auto&& cc = a3;auto&& cc1 = a2;const int& s1 = 100;const int&& s2 = 100;auto&& dd = s1;auto&& ee = s2;const auto&& x = 5;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li>第 2 行：<font color="orange">a1 </font>为右值引用，推导出的 <font color="orange">bb </font>为<font color="orange">左值引用</font>类型</li><li>第 3 行：<font color="orange">5 </font>为右值，推导出的 <font color="orange">bb1 </font>为<font color="orange">右值引用</font>类型</li><li>第 7 行：<font color="orange">a3 </font>为左值引用，推导出的 <font color="orange">cc </font>为<font color="orange">左值引用</font>类型</li><li>第 8 行：<font color="orange">a2 </font>为左值，推导出的 <font color="orange">cc1 </font>为<font color="orange">左值引用</font>类型</li><li>第 12 行：<font color="orange">s1 </font>为常量左值引用，推导出的 <font color="orange">dd </font>为<font color="orange">常量左值引用</font>类型</li><li>第 13 行：<font color="orange">s2 </font>为常量右值引用，推导出的 <font color="orange">ee </font>为<font color="orange">常量左值引用</font>类型</li><li>第 15 行：<font color="orange">x </font>为右值引用，不需要推导，只能通过右值初始化</li></ul><pre class="line-numbers language-C++"><code class="language-C++">#include <iostream>using namespace std;void printValue(int &i){    cout << "l-value: " << i << endl;}void printValue(int &&i){    cout << "r-value: " << i << endl;}void forward(int &&k){    printValue(k);}int main(){    int i = 520;    printValue(i);    printValue(1314);    forward(250);    return 0;};<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>测试代码输出的结果如下:</p><pre class="line-numbers language-C++"><code class="language-C++">l-value: 520r-value: 1314l-value: 250<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>根据测试代码可以得知，编译器会根据传入的参数的类型（左值还是右值）调用对应的重置函数（printValue），函数 <font color="orange">forward ()</font> 接收的是一个右值，但是在这个函数中调用函数 printValue () 时，参数 <font color="orange">k </font>变成了一个命名对象，编译器会将其当做左值来处理。</p><p>最后总结一下关于 &amp;&amp; 的使用：</p><ul><li><font color="red">左值和右值是独立于他们的类型的，右值引用类型可能是左值也可能是右值。</font></li><li><font color="red">编译器会将已命名的右值引用视为左值，将未命名的右值引用视为右值。</font></li><li><font color="red">auto&amp;&amp;或者函数参数类型自动推导的T&amp;&amp;是一个未定的引用类型，它可能是左值引用也可能是右值引用类型，这取决于初始化的值类型（上面有例子）。</font></li><li><font color="red">通过右值推导 T&amp;&amp; 或者 auto&amp;&amp; 得到的是一个右值引用类型，其余都是左值引用类型。</font></li></ul>]]></content>
      
      
      <categories>
          
          <category> C++11新特性 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> C++11新特性 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>C++11新特性--自动类型推导</title>
      <link href="/2022/04/20/zi-dong-lei-xing-tui-dao/"/>
      <url>/2022/04/20/zi-dong-lei-xing-tui-dao/</url>
      
        <content type="html"><![CDATA[<p>在 C++11 中增加了很多新的特性，比如可以使用 <font color="orange"><code>auto</code></font> 自动推导变量的类型，还能够结合 <font color="orange"><code>decltype</code></font> 来表示函数的返回值。使用新的特性可以让我们写出更加简洁，更加现代的代码。</p><h2 id="auto"><a href="#auto" class="headerlink" title="auto"></a>auto</h2><p>在 C++11 之前 <font color="orange"><code>auto</code></font> 和<font color="orange"> <code>static</code> </font>是对应的，表示变量是自动存储的，但是非 static 的局部变量默认都是自动存储的，因此这个关键字变得非常鸡肋，在 C++11 中他们赋予了新的含义，使用这个关键字能够像别的语言一样<font color="red"><code>自动推导出变量的实际类型</code></font>。</p><h4 id="推导规则"><a href="#推导规则" class="headerlink" title="推导规则"></a>推导规则</h4><p>C++11 中 <font color="orange"><code>auto</code></font> 并不代表一种实际的数据类型，只是一个类型声明的 <font color="cornflowerblue"><code>“占位符”</code></font>，<font color="orange"><code>auto</code></font> 并不是万能的在任意场景下都能够推导出变量的实际类型，<font color="red">使用auto声明的变量必须要进行初始化，以让编译器推导出它的实际类型，在编译时将auto占位符替换为真正的类型</font>。使用语法如下：</p><pre class="line-numbers language-C++"><code class="language-C++"><font color='orange'>`auto`</font> 变量名 = 变量值;<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><hr><p>根据上述语法，来列举一些简单的例子：</p><pre class="line-numbers language-C++"><code class="language-C++"><font color='orange'>`auto`</font> x = 3.14;      // x 是浮点型 double<font color='orange'>`auto`</font> y = 520;       // y 是整形 int<font color='orange'>`auto`</font> z = 'a';       // z 是字符型 char<font color='orange'>`auto`</font> nb;            // error，变量必须要初始化<font color='orange'>`auto`</font> double nbl;    // 语法错误, 不能修改数据类型   <span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>不仅如此，<font color="orange"><code>auto</code></font> 还可以和<font color="orange"><code>指针</code></font>、<font color="orange"><code>引用</code></font>结合起来使用也可以带上 <font color="orange"><code>const</code></font>、<font color="orange"><code>volatile</code></font> 限定符，在不同的场景下有对应的推导规则，规则内容如下：</p><ul><li><font color="red">当变量不是指针或者引用类型时，推导的结果中不会保留 const、volatile 关键字</font></li><li><font color="red">当变量是指针或者引用类型时，推导的结果中会保留 const、volatile 关键字</font></li></ul><hr><p>先来看一组变量带指针和引用并使用 <font color="orange"><code>auto</code></font> 进行类型推导的例子：</p><pre class="line-numbers language-C++"><code class="language-C++">int temp = 110;<font color='orange'>`auto`</font> *a = &temp;<font color='orange'>`auto`</font> b = &temp;<font color='orange'>`auto`</font> &c = temp;<font color='orange'>`auto`</font> d = temp;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>变量 <font color="orange"><code>a</code> </font>的数据类型为<font color="orange"><code>int*</code></font>，因此 <font color="orange"><code>auto</code></font>  关键字被推导为 <font color="orange"><code>int类型</code></font><br>变量 <font color="orange"><code>b</code> </font>的数据类型为 <font color="orange"><code>int*</code></font>，因此 <font color="orange"><code>auto</code></font> 关键字被推导为  <font color="orange"><code>int*</code></font> 类型<br>变量 <font color="orange"><code>c</code> </font> 的数据类型为<font color="orange"> int&amp;</font>，因此 <font color="orange"><code>auto</code></font> 关键字被推导为  <font color="orange"><code>int类型</code></font><br>变量 <font color="orange"><code>d</code> </font> 的数据类型为 <font color="orange"><code>int</code></font>，因此 <font color="orange"><code>auto</code></font> 关键字被推导为  <font color="orange"><code>int</code></font>类型</p><p>在来看一组带 <font color="orange"><code>const</code></font> 限定的变量，使用 <font color="orange"><code>auto</code></font> 进行类型推导的例子:</p><pre class="line-numbers language-C++"><code class="language-C++">int tmp = 250;const <font color='orange'>`auto`</font> a1 = tmp;<font color='orange'>`auto`</font> a2 = a1;const <font color='orange'>`auto`</font> &a3 = tmp;<font color='orange'>`auto`</font> &a4 = a3;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>变量 <font color="orange"><code>a1</code> </font> 的数据类型为 <font color="orange"><code>const int</code></font>，因此 <font color="orange"><code>auto</code></font> 关键字被推导为 <font color="orange"><code>int</code></font> 类型<br>变量 <font color="orange"><code>a2</code> </font> 的数据类型为  <font color="orange"><code>int</code></font> ，但是 <font color="orange"><code>a2</code> </font> 没有声明为指针或引用因此 <font color="orange"><code>const</code></font>  属性被去掉，<font color="orange"><code>auto</code></font> 被推导为 <font color="orange"><code>int</code></font> 类型<br>变量 <font color="orange"><code>a3</code> </font>的数据类型为  <font color="orange"><code>const int&amp;</code></font> ，<font color="orange"><code>a2</code> </font>被声明为引用因此 <font color="orange"><code>const</code></font>属性被保留，<font color="orange"><code>auto</code></font> 关键字被推导为 <font color="orange"><code>int</code></font>  类型<br>变量 <font color="orange"><code>a4</code> </font> 的数据类型为 <font color="orange"><code>const int&amp;</code></font> ，<font color="orange"><code>a2</code> </font> 被声明为引用因此 <font color="orange"><code>const</code></font>属性被保留，<font color="orange"><code>auto</code></font> 关键字被推导为 <font color="orange"><code>const int</code></font> 类型</p><hr><h4 id="auto的限制"><a href="#auto的限制" class="headerlink" title="auto的限制"></a>auto的限制</h4><p><font color="orange"><code>auto</code></font> 关键字并不是万能的，在以下这些场景中是不能完成类型推导的：</p><ul><li>不能作为函数参数使用。因为<font color="red"><code>只有在函数调用的时候才会给函数参数传递实参(auto没有在定义时初始化)</code></font>，<font color="orange"><code>auto</code></font> 要求必须要给修饰的变量赋值，因此二者矛盾。</li></ul><pre class="line-numbers language-C++"><code class="language-C++">int func(auto a, auto b)// error{    cout << "a: " << a <<", b: " << b << endl;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><ul><li>不能用于类的非静态成员变量的初始化<font color="red"><code>（类的静态非常量成员不允许在类内部直接初始化</code>）</font></li></ul><pre class="line-numbers language-C++"><code class="language-C++">class Test{    auto v1 = 0;                    // error    static auto v2 = 0;             // error,类的静态非常量成员不允许在类内部直接初始化    static const auto v3 = 10;      // ok}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li>不能使用<font color="orange"><code>auto</code></font> 关键字定义数组</li></ul><pre class="line-numbers language-C++"><code class="language-C++">int func(){    int array[] = {1,2,3,4,5};  // 定义数组    auto t1 = array;            // ok, t1被推导为 int* 类型    auto t2[] = array;          // error, auto无法定义数组    auto t3[] = {1,2,3,4,5};;   // error, auto无法定义数组}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li>无法使用 auto 推导出模板参数</li></ul><pre class="line-numbers language-C++"><code class="language-C++">template <typename T>struct Test{}int func(){    Test<double> t;    Test<auto> t1 = t;           // error, 无法推导出模板类型    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="auto的应用"><a href="#auto的应用" class="headerlink" title="auto的应用"></a>auto的应用</h4><p>了解了 <font color="orange"><code>auto</code></font> 的限制之后，我们就可以避开这些场景快乐的编程了，下面列举几个比较常用的场景：</p><ul><li><p><font color="red"><code>用于STL的容器遍历</code></font></p><p>在 C++11 之前，定义了一个stl 容器之后，遍历的时候常常会写出这样的代码：</p></li></ul><pre class="line-numbers language-C++"><code class="language-C++">#include <map>int main(){    map<int, string> person;    map<int, string>::iterator it = person.begin();    for (; it != person.end(); ++it)    {        // do something    }    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>可以看到在定义迭代器变量 it 的时候代码是很长的，写起来就很麻烦，使用了 auto 之后，就变得清爽了不少：</p><pre class="line-numbers language-C++"><code class="language-C++">#include <map>int main(){    map<int, string> person;    // 代码简化    for (auto it = person.begin(); it != person.end(); ++it)    {        // do something    }    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li><font color="red"><code>用于泛型编程</code></font>，在使用模板的时候，很多情况下我们不知道变量应该定义为什么类型，比如下面的代码：</li></ul><pre class="line-numbers language-C++"><code class="language-C++">#include <iostream>#include <string>using namespace std;class T1{public:    static int get()    {        return 10;    }};class T2{public:    static string get()    {        return "hello, world";    }};template <class A>void func(void){    auto val = A::get();    cout << "val: " << val << endl;}int main(){    func<T1>();    func<T2>();    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>在这个例子中定义了泛型函数 <font color="orange"><code>func</code></font>，在函数中调用了类 A 的静态方法 <font color="orange"><code>get ()</code></font> ，这个函数的返回值是不能确定的，如果不使用<font color="orange"> <code>auto</code></font>，就需要再定义一个模板参数，并且在外部调用时手动指定 <font color="orange"><code>get ()</code></font> 的返回值类型，具体代码如下：</p><pre class="line-numbers language-C++"><code class="language-C++">#include <iostream>#include <string>using namespace std;class T1{public:    static int get()    {        return 0;    }};class T2{public:    static string get()    {        return "hello, world";    }};template <class A, typename B>        // 添加了模板参数 Bvoid func(void){    B val = A::get();    cout << "val: " << val << endl;}int main(){    func<T1, int>();                  // 手动指定返回值类型 -> int    func<T2, string>();               // 手动指定返回值类型 -> string    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><hr><h3 id="decltype"><a href="#decltype" class="headerlink" title="decltype"></a>decltype</h3><p>在某些情况下，<font color="cornflowerblue">不需要或者不能定义变量，但是希望得到某种类型</font>，这时候就可以使用 C++11 提供的 <font color="orange"><code>decltype</code></font> 关键字了，<font color="red"><code>它的作用是在编译器编译的时候推导出一个表达式的类型</code></font>，语法格式如下：</p><pre class="line-numbers language-C++"><code class="language-C++">decltype (表达式)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p> <font color="orange"><code>decltype</code></font>  是 “declare type” 的缩写，意思是 “声明类型”。 <font color="orange"><code>decltype</code></font> 的推导是在编译期完成的，它只是用于表达式类型的推导，并不会计算表达式的值。来看一组简单的例子:</p><pre class="line-numbers language-C++"><code class="language-C++">int a = 10;decltype(a) b = 99;                 // b -> intdecltype(a+3.14) c = 52.13;         // c -> doubledecltype(a+b*c) d = 520.1314;       // d -> doubledecltype(a+b*c) e; //e -> doublee = 520.1314;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>可以看到 <font color="orange"><code>decltype</code></font>  推导的表达式可简单可复杂，在这一点上 <font color="orange"><code>auto</code> </font>是做不到的，<font color="orange"><code>auto</code> </font>只能推导已初始化的变量类型。</p><h4 id="推导规则-1"><a href="#推导规则-1" class="headerlink" title="推导规则"></a>推导规则</h4><ol><li><font color="red">表达式为普通变量或者普通表达式或者类表达式，在这种情况下，使用 decltype 推导出的类型和表达式的类型是一致的。</font></li></ol><pre class="line-numbers language-C++"><code class="language-C++">#include <iostream>#include <string>using namespace std;class Test{public:    string text;    static const int value = 110;};int main(){    int x = 99;    const int &y = x;    decltype(x) a = x;    decltype(y) b = x;    decltype(Test::value) c = 0;    Test t;    decltype(t.text) d = "hello, world";    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li>变量 <font color="orange"><code>a</code></font>  被推导为 int 类型</li><li>变量 <font color="orange"><code>b</code></font> 被推导为 <font color="orange"><code>const int &amp;</code></font> 类型</li><li>变量 <font color="orange"><code>c</code></font>  被推导为 <font color="orange"><code>const int </code></font> 类型</li><li>变量 <font color="orange"><code>d</code></font>  被推导为 <font color="orange"><code>string</code></font>  类型</li></ul><ol start="2"><li><font color="red">表达式是函数调用，使用 decltype 推导出的类型和函数返回值一致。</font></li></ol><pre class="line-numbers language-c++"><code class="language-c++">class Test{...};//函数声明int func_int();                 // 返回值为 intint& func_int_r();              // 返回值为 int&int&& func_int_rr();            // 返回值为 int&&const int func_cint();          // 返回值为 const intconst int& func_cint_r();       // 返回值为 const int&const int&& func_cint_rr();     // 返回值为 const int&&const Test func_ctest();        // 返回值为 const Test//decltype类型推导int n = 100;decltype(func_int()) a = 0;decltype(func_int_r()) b = n;decltype(func_int_rr()) c = 0;decltype(func_cint())  d = 0;decltype(func_cint_r())  e = n;decltype(func_cint_rr()) f = 0;decltype(func_ctest()) g = Test();<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li>变量 <font color="orange"><code>a</code></font>  被推导为 <font color="orange"><code>int</code></font>  类型</li><li>变量 <font color="orange"><code>b</code></font>  被推导为 <font color="orange"><code>int&nbsp;&amp;</code></font>   类型</li><li>变量 <font color="orange"><code>c</code></font>  被推导为 <font color="orange"><code>int&nbsp;&amp;&amp;</code></font>   类型</li><li>变量 <font color="orange"><code>d</code></font>  被推导为 <font color="orange"><code>int</code></font>  类型</li><li>变量 <font color="orange"><code>e</code></font>  被推导为 <font color="orange"><code>const int&nbsp;&amp;</code></font>   类型</li><li>变量 <font color="orange"><code>f</code></font>  被推导为 <font color="orange"><code>const int &amp;&amp;</code></font>   类型</li><li>变量 <font color="orange"><code>g</code></font>  被推导为 <font color="orange"><code>const Test</code></font>   类型</li></ul><p>函数 <font color="orange">func_cint ()</font> 返回的是一个纯右值（在表达式执行结束后不再存在的数据，也就是临时性的数据），<font color="orange">对于纯右值而言，只有类类型可以携带const、volatile限定符，除此之外需要忽略掉这两个限定符</font>，因此推导出的变量 d 的类型为 <font color="orange">int</font> 而不是 <font color="orange">const int</font>。</p><ol start="3"><li><font color="red">表达式是一个左值，或者被括号 ( ) 包围，使用 decltype 推导出的是表达式类型的引用（如果有 const、volatile 限定符不能忽略）。</font></li></ol><pre class="line-numbers language-c++"><code class="language-c++">#include <iostream>#include <vector>using namespace std;class Test{public:    int num;};int main() {    const Test obj; //const修饰的类对象其成员也别const修饰    //带有括号的表达式    decltype(obj.num) a = 0;    decltype((obj.num)) b = a;    //加法表达式    int n = 0, m = 0;    decltype(n + m) c = 0;    decltype(n = n + m) d = n;    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><font color="orange">obj.num</font> 为类的成员访问表达式，符合场景 1，因此 <font color="orange">a</font> 的类型为 <font color="orange">int</font><br><font color="orange">obj.num</font> 带有括号，符合场景 3，因此 <font color="orange">b</font> 的类型为 <font color="orange">const int&amp;</font>。<br><font color="orange">n+m</font> 得到一个右值，符合场景 1，因此 c 的类型为 <font color="orange">int</font><br><font color="orange">n=n+m</font> 得到一个左值 <font color="orange">n</font>，符合场景 3，因此 <font color="orange">d </font>的类型为 <font color="orange">int&amp;</font></p><hr><h4 id="decltype的应用"><a href="#decltype的应用" class="headerlink" title="decltype的应用"></a>decltype的应用</h4><p>关于 <font color="orange">decltype </font>的应用多出现在泛型编程中。比如我们编写一个类模板，在里边添加遍历容器的函数，操作如下：</p><pre class="line-numbers language-C++"><code class="language-C++">#include <list>using namespace std;template <class T>class Container{public:    void func(T& c)    {        for (m_it = c.begin(); m_it != c.end(); ++m_it)        {            cout << *m_it << " ";        }        cout << endl;    }private:    ??? m_it;  // 这里不能确定迭代器类型};int main(){    const list<int> lst;    Container<const list<int>> obj;    obj.func(lst);    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>在程序的第 17 行出了问题，关于迭代器变量一共有两种类型：<font color="orange">只读（T::const_iterator）</font>和<font color="orange">读写（T::iterator）</font>，有了 <font color="orange">decltype </font>就可以完美的解决这个问题了，当 <font color="orange">T</font> 是一个 非 <font color="orange">const </font>容器得到一个<font color="orange"> T::iterator</font>，当 <font color="orange">T </font>是一个 <font color="orange">const </font>容器时就会得到一个 <font color="orange">T::const_iterator</font>。</p><pre class="line-numbers language-C++"><code class="language-C++">#include <list>#include <iostream>using namespace std;template <class T>class Container{public:    void func(T& c)    {        for (m_it = c.begin(); m_it != c.end(); ++m_it)        {            cout << *m_it << " ";        }        cout << endl;    }private:    decltype(T().begin()) m_it;  // 这里不能确定迭代器类型};int main(){    const list<int> lst{ 1,2,3,4,5,6,7,8,9 };    Container<const list<int>> obj;    obj.func(lst);    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p><font color="orange">decltype(T().begin()) </font>这种写法在 vs2017/vs2019 下测试可用完美运行。</p><h4 id="返回类型后置"><a href="#返回类型后置" class="headerlink" title="返回类型后置"></a>返回类型后置</h4><p>在泛型编程中，可能需要通过参数的运算来得到返回值的类型，比如下面这个场景：</p><pre class="line-numbers language-C++"><code class="language-C++">#include <iostream>using namespace std;// R->返回值类型, T->参数1类型, U->参数2类型template <typename R, typename T, typename U>R add(T t, U u){    return t + u;}int main(){    int x = 520;    double y = 13.14;    // auto z = add<decltype(x + y), int, double>(x, y);    auto z = add<decltype(x + y)>(x, y);// 简化之后的写法    cout << "z: " << z << endl;    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>关于返回值，从上面的代码可以推断出和表达式 <font color="orange">t+u </font>的结果类型是一样的，因此可以通过通过 <font color="orange">decltype </font>进行推导，关于模板函数的参数<font color="orange"> t</font> 和 <font color="orange">u</font> 可以通过实参自动推导出来，因此在程序中就也可以不写。虽然通过上述方式问题被解决了，但是解决方案有点过于理想化，因为对于调用者来说，是不知道函数内部执行了什么样的处理动作的。</p><p>因此如果要想解决这个问题就得直接在 <font color="orange">add </font>函数身上做文章，先来看第一种写法：</p><pre class="line-numbers language-C++"><code class="language-C++">template <typename T, typename U>decltype(t+u) add(T t, U u){    return t + u;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>当我们在编译器中将这几行代码改出来后就直接报错了，因此 <font color="orange">decltype</font> 中的 <font color="orange">t </font>和 <font color="orange">u </font>都是函数参数，直接这样写相当于变量还没有定义就直接用上了，这时候变量还不存在，有点心急了。</p><hr><p><font color="orange">在C++11中增加了返回类型后置语法，说明白一点就是将decltype和auto结合起来完成返回类型的推导</font>。其语法格式如下:</p><pre class="line-numbers language-C++"><code class="language-C++">// 符号 -> 后边跟随的是函数返回值的类型auto func(参数1, 参数2, ...) -> decltype(参数表达式)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>通过对上述返回类型后置语法代码的分析，得到结论：<font color="orange">auto 会追踪 decltype() 推导出的类型</font>，因此上边的 <font color="orange">add() </font>函数可以做如下的修改：</p><pre class="line-numbers language-C++"><code class="language-C++">#include <iostream>using namespace std;template <typename T, typename U>// 返回类型后置语法auto add(T t, U u) -> decltype(t+u) {    return t + u;}int main(){    int x = 520;    double y = 13.14;    // auto z = add<int, double>(x, y);    auto z = add(x, y);// 简化之后的写法    cout << "z: " << z << endl;    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>为了进一步说明这个语法，我们再看一个例子：</p><pre class="line-numbers language-C++"><code class="language-C++">#include <iostream>using namespace std;int& test(int &i){    return i;}double test(double &d){    d = d + 100;    return d;}template <typename T>// 返回类型后置语法auto myFunc(T& t) -> decltype(test(t)){    return test(t);}int main(){    int x = 520;    double y = 13.14;    // auto z = myFunc<int>(x);    auto z = myFunc(x);             // 简化之后的写法    cout << "z: " << z << endl;    // auto z = myFunc<double>(y);    auto z1 = myFunc(y);            // 简化之后的写法    cout << "z1: " << z1 << endl;    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>在这个例子中，通过 <font color="orange">decltype </font>结合返回值后置语法很容易推导出来 <font color="orange">test(t) </font>函数可能出现的返回值类型，并将其作用到了函数 <font color="orange">myFunc() </font>上。</p><pre class="line-numbers language-C++"><code class="language-C++">// 输出结果z: 520z1: 113.14<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      <categories>
          
          <category> C++11新特性 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> C++11新特性 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>C++11新特性--constexpr</title>
      <link href="/2022/04/20/chang-liang-biao-da-shi/"/>
      <url>/2022/04/20/chang-liang-biao-da-shi/</url>
      
        <content type="html"><![CDATA[<h2 id="修饰常量表达式–constexpr"><a href="#修饰常量表达式–constexpr" class="headerlink" title="修饰常量表达式–constexpr"></a>修饰常量表达式–constexpr</h2><h3 id="const"><a href="#const" class="headerlink" title="const"></a>const</h3><p>在 C++11 之前只有 <font color="red"><code>const</code></font> 关键字，从功能上来说这个关键字有双重语义：<font color="red">变量只读，修饰常量</font>，举一个简单的例子：</p><pre class="line-numbers language-C++"><code class="language-C++">void func(const int num){    const int count = 24;    int array[num];            // error，num是一个只读变量，不是常量    int array1[count];         // ok，count是一个常量    int a1 = 520;    int a2 = 250;    const int& b = a1;    b = a2;                         // error    a1 = 1314;    cout << "b: " << b << endl;     // 输出结果为1314}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li>函数 <font color="orange">void func(const int num)</font> 的参数 <font color="orange">num </font>表示这个变量是只读的，但不是常量（<font color="orange"><code>num根据实参传入改变而改变</code></font>），因此使用 <font color="orange">int array[num];</font> 这种方式定义一个数组，<font color="red">编译器是会报错的</font>，提示 <font color="orange">num</font>  不可用作为常量来使用。</li><li><font color="orange">const int count = 24;</font> 中的 <font color="orange">count </font>却是一个常量，因此可以使用这个常量来定义一个静态数组。</li></ul><p>另外，<font color="orange">变量只读并不等价于常量</font>，二者是两个概念不能混为一谈，分析一下这句测试代码 <font color="orange">const int&amp; b = a1;</font>：</p><ul><li><p><font color="orange">b</font> 是一个常量的引用，所以 b 引用的变量是不能被修改的，也就是说<font color="orange"> b = a2;</font> 这句代码语法是错误的。</p></li><li><p>在<font color="orange"> const</font> 对于变量<font color="orange"> a1</font> 是没有任何约束的，<font color="orange">a1</font> 的值变了 <font color="orange">b</font> 的值也就变了</p></li><li><p>引用 <font color="orange">b</font> 是只读的，但是并不能保证它的值是不可改变的，也就是说它不是常量。</p></li></ul><hr><h3 id="constexpr"><a href="#constexpr" class="headerlink" title="constexpr"></a>constexpr</h3><p>在 C++11 中添加了一个新的关键字 <font color="orange">constexpr</font>，这个关键字是用来修饰常量表达式的。<font color="orange">所谓常量表达式，指的就是由多个（≥1）常量（值不会改变）组成并且在编译过程中就得到计算结果的表达式。</font></p><p>在介绍<font color="orac"> gcc/g++ 工作流程</font>的时候说过，C++ 程序从编写完毕到执行分为四个阶段：<font color="orange">预处理</font>、<font color="orange"> 编译</font>、<font color="orange">汇编</font>和<font color="orange">链接 </font>4 个阶段，得到可执行程序之后就可以运行了。需要额外强调的是，<font color="orange">常量表达式和非常量表达式的计算时机不同，非常量表达式只能在程序运行阶段计算出结果，但是常量表达式的计算往往发生在程序的编译阶段，这可以极大提高程序的执行效率</font>，因为<font color="red">表达式只需要在编译阶段计算一次，节省了每次程序运行时都需要计算一次的时间</font>。</p><p>那么问题来了，编译器如何识别表达式是不是常量表达式呢？在 C++11 中添加了 <font color="orange">constexpr</font> 关键字之后就可以在程序中使用它来修改常量表达式，用来提高程序的执行效率。在使用中建议将 <font color="orange">const</font> 和 <font color="orange">constexpr</font> 的功能区分开，即凡是表达“只读”语义的场景都使用 const，表达“常量”语义的场景都使用 <font color="orange">constexpr</font>。</p><p>在定义常量时，<font color="orange">const</font> 和 <font color="orange">constexpr </font>是等价的，都可以在程序的编译阶段计算出结果，例如：</p><pre class="line-numbers language-c++"><code class="language-c++">const int m = f();  // 不是常量表达式，m的值只有在运行时才会获取。const int i=520;    // 是一个常量表达式const int j=i+1;    // 是一个常量表达式constexpr int i=520;    // 是一个常量表达式constexpr int j=i+1;    // 是一个常量表达式<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>对于 C++ 内置类型的数据，可以直接用<font color="orange"> constexpr</font> 修饰，但如果是自定义的数据类型（用 <font color="orange">struct </font>或者<font color="orange"> class</font> 实现），直接用 <font color="orange">constexpr</font> 修饰是不行的，只能修饰自定义数据类型的<font color="cornflowerblue">实例对象</font>。</p><pre class="line-numbers language-C++"><code class="language-C++">// 此处的constexpr修饰是无效的constexpr struct Test{    int id;    int num;};<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>如果要定义一个结构体 / 类常量对象，可以这样写：</p><pre class="line-numbers language-C++"><code class="language-C++">struct Test{    int id;    int num;};int main(){    constexpr Test t{ 1, 2 };    constexpr int id = t.id;    constexpr int num = t.num;    // error，不能修改常量    t.num += 100;    cout << "id: " << id << ", num: " << num << endl;    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>在第 13行的代码中 <font color="orange">t.num += 100;</font> 的操作是错误的，对象 t 是一个常量，因此它的成员也是常量，常量是不能被修改的。</p><h2 id="常量表达式函数"><a href="#常量表达式函数" class="headerlink" title="常量表达式函数"></a>常量表达式函数</h2><p>为了提高 C++ 程序的执行效率，我们可以将程序中值不需要发生变化的变量定义为常量，也可以使用<font color="orange"> constexpr </font>修饰函数的返回值，这种函数被称作<font color="orange">常量表达式函数</font>，这些函数主要包括以下几种：<font color="red">普通函数/类成员函数、类的构造函数、模板函数</font>。</p><h3 id="修饰普通函数-x2F-类成员函数"><a href="#修饰普通函数-x2F-类成员函数" class="headerlink" title="修饰普通函数/类成员函数"></a>修饰普通函数/类成员函数</h3><p><font color="orange">constexpr </font>并不能修改任意函数的返回值，使这些函数成为常量表达式函数，必须要满足以下几个条件：</p><ol><li><font color="red">函数必须要有返回值，并且 return 返回的表达式必须是常量表达式。</font></li></ol><pre class="line-numbers language-C++"><code class="language-C++">// error，不是常量表达式函数constexpr void func1(){    int a = 100;    cout << "a: " << a << endl;}// error，不是常量表达式函数constexpr int func1(){    int a = 100;    return a;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li>函数 <font color="orange">func1()</font> 没有返回值，不满足常量表达式函数要求</li><li>函数 <font color="orange">func2()</font> 返回值不是常量表达式，不满足常量表达式函数要求</li></ul><ol start="2"><li><font color="red">函数在使用之前，必须有对应的定义语句</font>。</li></ol><pre class="line-numbers language-c++"><code class="language-c++">#include <iostream>using namespace std;constexpr int func1();int main(){    constexpr int num = func1();// error 函数在使用之前，必须有对应的定义语句    return 0;}constexpr int func1(){    constexpr int a = 100;    return a;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li>在测试程序<font color="orange">&nbsp;<code>constexpr int num = func1()</code>;</font> 中，还没有定义 <font color="orange"><code>func1(</code>)</font> 就直接调用了，应该将 <font color="orange"><code>func1()</code> </font>函数的定义放到 <font color="orange"><code>main()</code> </font>函数的上边。</li></ul><ol start="3"><li><font color="red">整个函数的函数体中，不能出现非常量表达式之外的语句）</font><font color="orange">（using 指令、typedef 语句以及 static_assert 断言、return 语句除外)</font>。</li></ol><pre class="line-numbers language-C++"><code class="language-C++">// errorconstexpr int func1(){    constexpr int a = 100;    constexpr int b = 10;    for (int i = 0; i < b; ++i)    {        cout << "i: " << i << endl;    }    return a + b;}// okconstexpr int func2(){    using mytype = int;    constexpr mytype a = 100;    constexpr mytype b = 10;    constexpr mytype c = a * b;    return c - (a + b);}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><ul><li>因为 <font color="orange"><code>func1()</code></font> 是一个常量表达式函数，在函数体内部是不允许出现非常量表达式以外的操作，因此函数体内部的 <font color="orange"><code>for</code></font> 循环是一个非法操作。</li></ul><blockquote><p>以上三条规则不仅对应普通函数适用，对应类的成员函数也是适用的：</p></blockquote><h3 id="修饰模板函数"><a href="#修饰模板函数" class="headerlink" title="修饰模板函数"></a>修饰模板函数</h3><p>C++11 语法中，<font color="orange"><code>constexpr</code></font> 可以修饰函数模板，但由于模板中类型的不确定性，因此函数模板实例化后的模板函数是否符合常量表达式函数的要求也是不确定的(<font color="orange"><code>主要看，模板函数调用时传递的实参是否是常量</code></font>)。<font color="red">如果 constexpr 修饰的模板函数实例化结果不满足常量表达式函数的要求，则 constexpr 会被自动忽略，即该函数就等同于一个普通函数。</font></p><pre class="line-numbers language-C++"><code class="language-C++">#include <iostream>using namespace std;struct Person {    const char* name;    int age;};// 定义函数模板template<typename T>constexpr T dispaly(T t) {    return t;}int main(){    struct Person p { "luffy", 19 };    //普通函数    struct Person ret = dispaly(p);    cout << "luffy's name: " << ret.name << ", age: " << ret.age << endl;    //常量表达式函数    constexpr int ret1 = dispaly(250);    cout << ret1 << endl;    constexpr struct Person p1 { "luffy", 19 };    constexpr struct Person p2 = dispaly(p1);    cout << "luffy's name: " << p2.name << ", age: " << p2.age << endl;    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>在上面示例程序中定义了一个函数模板 <font color="orange"><code>display()</code></font>，但由于其返回值类型未定，因此在实例化之前无法判断其是否符合常量表达式函数的要求：</p><ul><li><font color="orange"><code>struct Person ret = dispaly(p);</code> </font>由于参数 p 是变量，所以实例化后的函数不是常量表达式函数，此时 <font color="orange"><code>constexpr</code></font> 是无效的</li><li><font color="orange"><code>constexpr int ret1 = dispaly(250);</code></font>;参数是常量，符合常量表达式函数的要求，此时 <font color="orange"><code>constexpr</code></font> 是有效的</li><li><font color="orange"><code>constexpr struct Person p2 = dispaly(p1);</code></font> 参数是常量，符合常量表达式函数的要求，此时 <font color="orange"><code>constexpr</code></font> 是有效的</li></ul><h3 id="修饰构造函数"><a href="#修饰构造函数" class="headerlink" title="修饰构造函数"></a>修饰构造函数</h3><p>如果想用<font color="orange"><code>直接得到一个常量对象</code></font>，也可以使用 constexpr 修饰一个构造函数，这样就可以得到一个常量构造函数了。常量构造函数有一个要求：<font color="red"><code>构造函数的函数体必须为空，并且必须采用初始化列表的方式为各个成员赋值。</code></font></p><pre class="line-numbers language-C++"><code class="language-C++">#include <iostream>using namespace std;struct Person {    constexpr Person(const char* p, int age) :name(p), age(age)    {    }    const char* name;    int age;};int main(){    constexpr struct Person p1("luffy", 19);    cout << "luffy's name: " << p1.name << ", age: " << p1.age << endl;    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre>]]></content>
      
      
      <categories>
          
          <category> C++11新特性 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> C++11新特性 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>C++11新特性--nullPtr</title>
      <link href="/2022/04/20/kong-zhi-zhen-lei-xing/"/>
      <url>/2022/04/20/kong-zhi-zhen-lei-xing/</url>
      
        <content type="html"><![CDATA[<h2 id="空指针类型–nullptr"><a href="#空指针类型–nullptr" class="headerlink" title="空指针类型–nullptr"></a>空指针类型–nullptr</h2><p>在 C++ 程序开发中，为了提高程序的健壮性，一般会在定义指针的同时完成初始化操作，或者在指针的指向尚未明确的情况下，都会给指针初始化为<span style="color:red;background:背景颜色;font-size:文字大小;font-family:字体;">NULL</span> ，避免产生<span style="color:red;background:背景颜色;font-size:文字大小;font-family:字体;">野指针（没有明确指向的指针，操作也这种指针极可能导致程序发生异常）</span>。C++98/03 标准中，将一个指针初始化为空指针的方式有 2 种：</p><pre class="line-numbers language-C++"><code class="language-C++">char *ptr = 0;char *ptr = NULL;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>在底层源码中 NULL 这个宏是这样定义的:</p><pre class="line-numbers language-C++"><code class="language-C++">#ifndef NULL    #ifdef __cplusplus        #define NULL 0    #else        #define NULL ((void *)0)    #endif#endif<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>也就是说如果源码是 C++ 程序 <span style="color:red;background:背景颜色;font-size:文字大小;font-family:字体;">NULL</span> 就是<span style="color:red;background:背景颜色;font-size:文字大小;font-family:字体;">0</span> ，如果是 C 程序  <span style="color:red;background:背景颜色;font-size:文字大小;font-family:字体;">NULL</span>  表示  <span style="color:red;background:背景颜色;font-size:文字大小;font-family:字体;">(void*)0</span> 。那么为什么要这样做呢？ 是由于 C++ 中，<span style="color:red;background:背景颜色;font-size:文字大小;font-family:字体;">void *&nbsp;</span>  类型无法隐式转换为其他类型的指针，此时使用 <span style="color:red;background:背景颜色;font-size:文字大小;font-family:字体;">0</span>  代替 <span style="color:red;background:背景颜色;font-size:文字大小;font-family:字体;">((void *)0)</span> ，用于解决空指针的问题。这个 0（0x0000 0000）表示的就是虚拟地址空间中的 0 地址，这块地址是只读的。</p><hr><p>C++ 中将 NULL 定义为字面常量 0，并不能保证在所有场景下都能很好的工作，比如，函数重载时，NULL 和 0 无法区分：</p><pre class="line-numbers language-C+_+"><code class="language-C+_+">#include <iostream>using namespace std;void func(char *p){    cout << "void func(char *p)" << endl;}void func(int p){    cout << "void func(int p)" << endl;}int main(){    func(NULL);   // 想要调用重载函数 void func(char *p)，但实际调用的是fun(0)    func(250);    // 想要调用重载函数 void func(int p)    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>测试代码打印的结果为：</p><pre class="line-numbers language-C++"><code class="language-C++">void func(int p)void func(int p)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>通过打印的结果可以看到，虽然调用 <font color="red">func(NULL)</font>; 最终链接到的还是 <font color="red">void func(int p)</font> 和预期是不一样的，其实这个原因前边已经说的很明白了，在 C++ 中 <font color="red">NULL</font> 和 <font color="red">0</font> 是等价的。</p><hr><p>出于兼容性的考虑，C++11 标准并没有对 NULL 的宏定义做任何修改，而是另其炉灶，引入了一个新的关键字 <font color="orange">nullptr</font>。<font color="orange">nullptr专用于初始化空类型指针，不同类型的指针变量都可以使用 nullptr 来初始化 </font>：</p><pre class="line-numbers language-c++"><code class="language-c++">int*    ptr1 = nullptr;char*   ptr2 = nullptr;double* ptr3 = nullptr;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>对应上面的代码编译器会分别将 <font color="red">nullptr</font> 隐式转换成<font color="red">int*</font>、<font color="red">char*</font>、以及 <font color="red">double*</font> 指针类型。</p><p>使用 <font color="red">nullptr </font>可以很完美的解决上边提到的函数重载问题:</p><pre class="line-numbers language-C++"><code class="language-C++">#include <iostream>using namespace std;void func(char *p){    cout << "void func(char *p)" << endl;}void func(int p){    cout << "void func(int p)" << endl;}int main(){    func(nullptr);    func(250);    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>测试代码输出的结果:</p><pre class="line-numbers language-c++"><code class="language-c++">void func(char *p)void func(int p)<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>通过输出的结果可以看出，<font color="red">nullptr 无法隐式转换为整形，但是可以隐式匹配指针类型。在 C++11 标准下，相比 NULL 和 0，使用 nullptr 初始化空指针可以令我们编写的程序更加健壮。</font></p>]]></content>
      
      
      <categories>
          
          <category> C++11新特性 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> C++11新特性 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>C++11新特性--原始字面量</title>
      <link href="/2022/04/19/zi-fu-chuan-yuan-shi-zi-mian-liang/"/>
      <url>/2022/04/19/zi-fu-chuan-yuan-shi-zi-mian-liang/</url>
      
        <content type="html"><![CDATA[<h2 id="字符串原始字面量–R”-”"><a href="#字符串原始字面量–R”-”" class="headerlink" title="字符串原始字面量–R”()”"></a>字符串原始字面量–R”()”</h2><h3 id="定义"><a href="#定义" class="headerlink" title="定义"></a>定义</h3><p>在 C++11 中添加了定义原始字符串的字面量，定义方式为：<span style="color:red;background:背景颜色;font-size:文字大小;font-family:字体;">R “xxx(原始字符串)xxx” 其中（）两边的字符串可以省略</span>。原始字面量 R 可以直接表示字符串的实际含义，而不需要额外对字符串做转义或连接等操作。</p><p>比如：编程过程中，使用的字符串中常带有一些特殊字符，对于这些字符往往要做专门的处理，使用了原始字面量就可以轻松的解决这个问题了，比如打印路径：</p><pre class="line-numbers language-c++"><code class="language-c++">#include<iostream>#include<string>using namespace std;int main(){    string str = "D:\hello\world\test.text";    cout << str << endl;    string str1 = "D:\\hello\\world\\test.text";    //通过第一个'\'来转义第二个'\'    cout << str1 << endl;    string str2 = R"(D:\hello\world\test.text)";    cout << str2 << endl;    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>输出结果为：</p><pre class="line-numbers language-C++"><code class="language-C++">D:helloworld    est.textD:\hello\world\test.textD:\hello\world\test.text<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><ul><li>在 D:\hello\world\test.text 中 \h 和 \w 转义失败，对应的字符会原样输出</li><li>在 D:\hello\world\test.text 中路径的间隔符为 \ 但是这个字符又是转义字符，因此需要使用转义字符将其转义，最终才能得到一个没有特殊含义的普通字符 \</li><li>在 R”(D:\hello\world\test.text)” 使用了原始字面量 R（）中的内容就是描述路径的原始字符串，无需做任何处理</li></ul><p>在 R”(D:\hello\world\test.text)” 使用了原始字面量 R（）中的内容就是描述路径的原始字符串，无需做任何处理</p><hr><h3 id="应用"><a href="#应用" class="headerlink" title="应用"></a>应用</h3><p>在 C++11 之前如果一个字符串分别写到了不同的行里边，需要加连接符，这种方式不仅繁琐，还破坏了表达式的原始含义，如果使用原始字面量就变得简单很多，很强直观，可读性强。</p><pre class="line-numbers language-C++"><code class="language-C++">#include<iostream>#include<string>using namespace std;int main(){    string str = "\        我是路飞!!!\         我是要成为海贼王的男人!!!\        ";    cout << str << endl;    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><hr><pre class="line-numbers language-C++"><code class="language-C++">#include<iostream>#include<string>using namespace std;int main(){    string str = R"(        我是路飞!!!        我是要成为海贼王的男人!!!        )";    cout << str << endl;    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><hr><p>最后强调一个细节：<span style="color:red;background:背景颜色;font-size:文字大小;font-family:字体;">在R “xxx(raw string)xxx” 中，原始字符串必须用括号（）括起来，括号的前后可以加其他字符串，所加的字符串会被忽略，并且加的字符串必须在括号两边同时出现</span>。</p><pre class="line-numbers language-markdown"><code class="language-markdown"><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>span</span><span class="token style-attr language-css"><span class="token attr-name"> <span class="token attr-name">style</span></span><span class="token punctuation">='</span><span class="token attr-value"><span class="token property">color</span><span class="token punctuation">:</span>文字颜色<span class="token punctuation">;</span><span class="token property">background</span><span class="token punctuation">:</span>背景颜色<span class="token punctuation">;</span><span class="token property">font-size</span><span class="token punctuation">:</span>文字大小<span class="token punctuation">;</span><span class="token property">font-family</span><span class="token punctuation">:</span>字体<span class="token punctuation">;</span></span><span class="token punctuation">'</span></span><span class="token punctuation">></span></span>文字<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>span</span><span class="token punctuation">></span></span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><pre class="line-numbers language-C++"><code class="language-C++">#include<iostream>#include<string>using namespace std;int main(){    string str1 = R"(D:\hello\world\test.text)";    cout << str1 << endl;    string str2 = R"luffy(D:\hello\world\test.text)luffy";    cout << str2 << endl;#if 0    // 语法错误，编译不通过    string str3 = R"luffy(D:\hello\world\test.text)robin";    cout << str3 << endl;#endif    return 0;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>测试代码输出的结果为：</p><pre class="line-numbers language-C++"><code class="language-C++">D:\hello\world\test.textD:\hello\world\test.text<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>通过输出的信息可以得到如下结论：<span style="color:red;background:背景颜色;font-size:文字大小;font-family:字体;">使用原始字面量 R “xxx (raw string) xxx”，（）两边的字符串在解析的时候是会被忽略的，因此一般不用指定。如果在（）前后指定了字符串，那么前后的字符串必须相同，否则会出现语法错误</span>。</p>]]></content>
      
      
      <categories>
          
          <category> C++11新特性 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> C++11新特性 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>MarkDown基础</title>
      <link href="/2022/04/15/markdown-jiao-cheng/"/>
      <url>/2022/04/15/markdown-jiao-cheng/</url>
      
        <content type="html"><![CDATA[<h1 id="MarkDown基础"><a href="#MarkDown基础" class="headerlink" title="MarkDown基础"></a>MarkDown基础</h1><p><a href="https://www.bilibili.com/video/av87982836#reply2366896129">基础篇视频讲解链接</a><br><a href="https://www.bilibili.com/video/av88551739/">画图篇视频讲解链接</a></p><h2 id="标题"><a href="#标题" class="headerlink" title="标题"></a>标题</h2><pre class="line-numbers language-markdown"><code class="language-markdown"><span class="token title important"><span class="token punctuation">#</span> 标题名字（井号的个数代表标题的级数）</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h1 id="一级标题使用1个"><a href="#一级标题使用1个" class="headerlink" title="一级标题使用1个#"></a>一级标题使用1个#</h1><h2 id="二级标题使用2个"><a href="#二级标题使用2个" class="headerlink" title="二级标题使用2个#"></a>二级标题使用2个#</h2><h3 id="三级标题使用3个"><a href="#三级标题使用3个" class="headerlink" title="三级标题使用3个#"></a>三级标题使用3个#</h3><h4 id="四级标题使4用个"><a href="#四级标题使4用个" class="headerlink" title="四级标题使4用个#"></a>四级标题使4用个#</h4><h5 id="五级标题使用5个"><a href="#五级标题使用5个" class="headerlink" title="五级标题使用5个#"></a>五级标题使用5个#</h5><h6 id="六级标题使用6个"><a href="#六级标题使用6个" class="headerlink" title="六级标题使用6个#"></a>六级标题使用6个#</h6><p>####### 最多支持六级标题#</p><h2 id="文字"><a href="#文字" class="headerlink" title="文字"></a>文字</h2><h3 id="删除线"><a href="#删除线" class="headerlink" title="删除线"></a>删除线</h3><pre class="line-numbers language-markdown"><code class="language-markdown">这就是 ~~删除线~~ (使用波浪号)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>这就是 <del>删除线</del> (使用波浪号)</p><h3 id="斜体"><a href="#斜体" class="headerlink" title="斜体"></a>斜体</h3><pre class="line-numbers language-markdown"><code class="language-markdown">这是用来 <span class="token italic"><span class="token punctuation">*</span>斜体<span class="token punctuation">*</span></span> 的 <span class="token italic"><span class="token punctuation">_</span>文本<span class="token punctuation">_</span></span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>这是用来 <em>斜体</em> 的 <em>文本</em></p><h3 id="加粗"><a href="#加粗" class="headerlink" title="加粗"></a>加粗</h3><pre class="line-numbers language-markdown"><code class="language-markdown">这是用来 <span class="token bold"><span class="token punctuation">**</span>加粗<span class="token punctuation">**</span></span> 的 <span class="token bold"><span class="token punctuation">__</span>文本<span class="token punctuation">__</span></span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>这是用来 <strong>加粗</strong> 的 <strong>文本</strong></p><h3 id="斜体-加粗"><a href="#斜体-加粗" class="headerlink" title="斜体+加粗"></a>斜体+加粗</h3><pre class="line-numbers language-markdown"><code class="language-markdown">这是用来 <span class="token bold"><span class="token punctuation">**</span>*斜体+加粗<span class="token punctuation">**</span></span>* 的 <span class="token bold"><span class="token punctuation">__</span>_文本<span class="token punctuation">__</span></span>_<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>这是用来 <em><strong>斜体+加粗</strong></em> 的 <em><strong>文本</strong></em></p><h3 id="下划线"><a href="#下划线" class="headerlink" title="下划线"></a>下划线</h3><p>下划线是HTML语法</p><p><code>下划线</code> <u>下划线(快捷键<code>command</code>+<code>u</code>，视频中所有的快捷键都是针对Mac系统，其他系统可自行查找，Windows下的为ctrl + u)</u></p><h3 id="高亮（需勾选扩展语法）"><a href="#高亮（需勾选扩展语法）" class="headerlink" title="高亮（需勾选扩展语法）"></a>高亮（需勾选扩展语法）</h3><pre class="line-numbers language-markdown"><code class="language-markdown">这是用来 ==斜体+加粗== 的文本<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>这是用来 ==斜体+加粗== 的文本</p><h3 id="下标（需勾选扩展语法）"><a href="#下标（需勾选扩展语法）" class="headerlink" title="下标（需勾选扩展语法）"></a>下标（需勾选扩展语法）</h3><pre class="line-numbers language-markdown"><code class="language-markdown">水 H~2~O 双氧水 H~2~O~2~ <span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>水 H<del>2</del>O </p><p>双氧水 H<del>2</del>O<del>2</del></p><h3 id="上标（需勾选扩展语法）"><a href="#上标（需勾选扩展语法）" class="headerlink" title="上标（需勾选扩展语法）"></a>上标（需勾选扩展语法）</h3><pre class="line-numbers language-markdown"><code class="language-markdown">面积 m^2^ 体积 m^3^<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>面积 m^2^<br>体积 m^3^</p><h3 id="表情符号"><a href="#表情符号" class="headerlink" title="表情符号"></a>表情符号</h3><p> Emoji 支持表情符号，你可以用系统默认的 Emoji 符号（ Windows 用户不一定支持，自己试下~）。 也可以用图片的表情，输入 <code>:</code> 将会出现智能提示。  </p><h4 id="一些表情例子"><a href="#一些表情例子" class="headerlink" title="一些表情例子"></a>一些表情例子</h4><pre class="line-numbers language-markdown"><code class="language-markdown">:smile: :laughing: :dizzy<span class="token italic"><span class="token punctuation">_</span>face: :sob: :cold<span class="token punctuation">_</span></span>sweat: :sweat<span class="token italic"><span class="token punctuation">_</span>smile:  :cry: :triumph: :heart<span class="token punctuation">_</span></span>eyes: :relaxed: :sunglasses: :weary::+1: :-1: :100: :clap: :bell: :gift: :question: :bomb: :heart: :coffee: :cyclone: :bow: :kiss: :pray: :sweat_drops: :hankey: :exclamation: :anger:<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p><span class="github-emoji"><span>😄</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/1f604.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span> <span class="github-emoji"><span>😆</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/1f606.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span> <span class="github-emoji"><span>😵</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/1f635.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span> <span class="github-emoji"><span>😭</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/1f62d.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span> <span class="github-emoji"><span>😰</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/1f630.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span> <span class="github-emoji"><span>😅</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/1f605.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span>  <span class="github-emoji"><span>😢</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/1f622.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span> <span class="github-emoji"><span>😤</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/1f624.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span> <span class="github-emoji"><span>😍</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/1f60d.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span> <span class="github-emoji"><span>☺</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/263a.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span> <span class="github-emoji"><span>😎</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/1f60e.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span> <span class="github-emoji"><span>😩</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/1f629.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span> :+1: :-1: <span class="github-emoji"><span>💯</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/1f4af.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span> <span class="github-emoji"><span>👏</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/1f44f.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span> <span class="github-emoji"><span>🔔</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/1f514.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span> <span class="github-emoji"><span>🎁</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/1f381.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span> <span class="github-emoji"><span>❓</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/2753.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span> <span class="github-emoji"><span>💣</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/1f4a3.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span> <span class="github-emoji"><span>❤</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/2764.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span> <span class="github-emoji"><span>☕</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/2615.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span> <span class="github-emoji"><span>🌀</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/1f300.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span> <span class="github-emoji"><span>🙇</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/1f647.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span> <span class="github-emoji"><span>💋</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/1f48b.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span> <span class="github-emoji"><span>🙏</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/1f64f.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span> <span class="github-emoji"><span>💦</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/1f4a6.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span> <span class="github-emoji"><span>💩</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/1f4a9.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span> <span class="github-emoji"><span>❗</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/2757.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span> <span class="github-emoji"><span>💢</span><img src="https://github.githubassets.com/images/icons/emoji/unicode/1f4a2.png?v8" aria-hidden="true" onerror="this.parent.classList.add('github-emoji-fallback')"></span></p><p>(  Mac: <code>control</code>+<code>command</code>+<code>space</code>点选)</p><h3 id="表格"><a href="#表格" class="headerlink" title="表格"></a>表格</h3><p>使用 <code>|</code> 来分隔不同的单元格，使用 <code>-</code> 来分隔表头和其他行：</p><pre class="line-numbers language-markdown"><code class="language-markdown"><span class="token title important">name | price<span class="token punctuation">---</span></span> | ---fried chicken | 19cola|5<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p>为了使 Markdown 更清晰，<code>|</code> 和 <code>-</code> 两侧需要至少有一个空格（最左侧和最右侧的 <code>|</code> 外就不需要了）。</p></blockquote><table><thead><tr><th>name</th><th>price</th></tr></thead><tbody><tr><td>fried chicken</td><td>19</td></tr><tr><td>cola</td><td>5</td></tr></tbody></table><p>为了美观，可以使用空格对齐不同行的单元格，并在左右两侧都使用 <code>|</code> 来标记单元格边界，在表头下方的分隔线标记中加入 <code>:</code>，即可标记下方单元格内容的对齐方式：</p><pre class="line-numbers language-markdown"><code class="language-markdown">|    name       | price || :------------ | :---: || fried chicken | 19    || cola          |  32   |<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><table><thead><tr><th align="left">name</th><th align="center">price</th></tr></thead><tbody><tr><td align="left">fried chicken</td><td align="center">19</td></tr><tr><td align="left">cola</td><td align="center">32</td></tr></tbody></table><p>使用快捷键<code>command</code>+<code>opt</code>+<code>T</code>更方便(段落→表格→插入表格，即可查看快捷键)</p><h2 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h2><pre class="line-numbers language-markdown"><code class="language-markdown"><span class="token blockquote punctuation">></span>“后悔创业”<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><blockquote><p>“后悔创业”</p></blockquote><pre class="line-numbers language-markdown"><code class="language-markdown"><span class="token blockquote punctuation">></span>也可以在引用中<span class="token blockquote punctuation">>></span>使用嵌套的引用<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><blockquote><p>也可以在引用中</p><blockquote><p>使用嵌套的引用</p></blockquote></blockquote><h2 id="列表"><a href="#列表" class="headerlink" title="列表"></a>列表</h2><h3 id="无序列表–符号-空格"><a href="#无序列表–符号-空格" class="headerlink" title="无序列表–符号 空格"></a>无序列表–符号 空格</h3><pre class="line-numbers language-markdown"><code class="language-markdown"><span class="token list punctuation">*</span> 可以使用 <span class="token code keyword">`*`</span> 作为标记<span class="token list punctuation">+</span> 也可以使用 <span class="token code keyword">`+`</span><span class="token list punctuation">-</span> 或者 <span class="token code keyword">`-`</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><ul><li>可以使用 <code>*</code> 作为标记</li></ul><ul><li>也可以使用 <code>+</code></li></ul><ul><li>或者 <code>-</code></li></ul><h3 id="有序列表–数字-空格"><a href="#有序列表–数字-空格" class="headerlink" title="有序列表–数字 . 空格"></a>有序列表–数字 <code>.</code> 空格</h3><pre class="line-numbers language-markdown"><code class="language-markdown"><span class="token list punctuation">1.</span> 有序列表以数字和 <span class="token code keyword">`.`</span> 开始；<span class="token list punctuation">3.</span> 数字的序列并不会影响生成的列表序列；<span class="token list punctuation">4.</span> 但仍然推荐按照自然顺序（1.2.3...）编写。<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><ol><li><p>有序列表以数字和 <code>.</code> 开始；</p></li><li><p>数字的序列并不会影响生成的列表序列；</p></li><li><p>但仍然推荐按照自然顺序（1.2.3…）编写。</p><pre class="line-numbers language-markdown"><code class="language-markdown">可以使用：数字\. 来取消显示为列表（用反斜杠进行转义）<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre></li></ol><h2 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h2><h3 id="代码块"><a href="#代码块" class="headerlink" title="代码块"></a>代码块</h3><pre class="line-numbers language-markdown"><code class="language-markdown">```语言名称<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><pre class="line-numbers language-java"><code class="language-java"> <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">void</span> <span class="token function">main</span><span class="token punctuation">(</span>String<span class="token punctuation">[</span><span class="token punctuation">]</span> args<span class="token punctuation">)</span> <span class="token punctuation">{</span>    <span class="token punctuation">}</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><h3 id="行内代码"><a href="#行内代码" class="headerlink" title="行内代码"></a>行内代码</h3><pre class="line-numbers language-markdown"><code class="language-markdown">也可以通过 `<span class="token code keyword">`，插入行内代码（`</span> 是 <span class="token code keyword">`Tab`</span> 键上边、数字 <span class="token code keyword">`1`</span> 键左侧的那个按键）：例如 <span class="token code keyword">`Markdown`</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p><code>Markdown</code></p><h3 id="转换规则"><a href="#转换规则" class="headerlink" title="转换规则"></a>转换规则</h3><p>代码块中的文本（包括 Markdown 语法）都会显示为原始内容</p><h2 id="分隔线"><a href="#分隔线" class="headerlink" title="分隔线"></a>分隔线</h2><p>可以在一行中使用三个或更多的 <code>*</code>、<code>-</code> 或 <code>_</code> 来添加分隔线（``）：</p><pre class="line-numbers language-markdown"><code class="language-markdown"><span class="token hr punctuation">***</span><span class="token hr punctuation">------</span><span class="token italic"><span class="token punctuation">_</span><span class="token punctuation">_</span><span class="token punctuation">_</span></span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><hr><hr><hr><h2 id="跳转"><a href="#跳转" class="headerlink" title="跳转"></a>跳转</h2><h3 id="外部跳转–超链接"><a href="#外部跳转–超链接" class="headerlink" title="外部跳转–超链接"></a>外部跳转–超链接</h3><p>格式为 <code>[link text](link)</code>。</p><pre class="line-numbers language-markdown"><code class="language-markdown"><span class="token url">[帮助文档](https://support.typora.io/Links/#faq)</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p><a href="https://support.typora.io/Links/#faq">帮助文档</a></p><h3 id="内部跳转–本文件内跳（Typora支持）"><a href="#内部跳转–本文件内跳（Typora支持）" class="headerlink" title="内部跳转–本文件内跳（Typora支持）"></a>内部跳转–本文件内跳（Typora支持）</h3><p>格式为 <code>[link text](#要去的目的地--标题）</code>。</p><pre class="line-numbers language-markdown"><code class="language-markdown"><span class="token url">[我想跳转](#饼图（Pie）)</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><blockquote><p>Open Links in Typora</p><p>You can use <code>command+click</code> (macOS), or <code>ctrl+click</code> (Linux/Windows) on links in Typora to jump to target headings, or open them in Typora, or open in related apps.</p></blockquote><p><a href="#%E9%A5%BC%E5%9B%BE%EF%BC%88Pie%EF%BC%89">我想跳转</a></p><h3 id="自动链接"><a href="#自动链接" class="headerlink" title="自动链接"></a>自动链接</h3><p>使用 <code>&lt;&gt;</code> 包括的 URL 或邮箱地址会被自动转换为超链接：</p><pre class="line-numbers language-markdown"><code class="language-markdown">&lt;https://www.baidu.com>&lt;123@email.com><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p><a href="https://www.baidu.com/">https://www.baidu.com</a></p><p><a href="mailto:123@email.com">123@email.com</a></p><h2 id="图片"><a href="#图片" class="headerlink" title="图片"></a>图片</h2><pre class="line-numbers language-markdown"><code class="language-markdown"><span class="token url">![自己起的图片名字](图片地址或者图片本地存储的路径)</span><span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h3 id="网上的图片"><a href="#网上的图片" class="headerlink" title="网上的图片"></a>网上的图片</h3><pre class="line-numbers language-markdown"><code class="language-markdown">![friedChicken](https://ss0.bdstatic.com/94oJfD<span class="token italic"><span class="token punctuation">_</span>bAAcT8t7mm9GUKT-xh<span class="token punctuation">_</span></span>/timg?image&amp;quality=100&amp;size=b4000_4000&amp;sec=1580814517&amp;di=2630beac440e5dab0e44c7286a3b2b61&amp;src=http://imgsrc.baidu.com/forum/w=580/sign=12c730c4ff03738dde4a0c2a831ab073/9497794f9258d1091818e6d6d858ccbf6d814d1b.jpg)<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p><img src="https://ss0.bdstatic.com/94oJfD_bAAcT8t7mm9GUKT-xh_/timg?image&amp;quality=100&amp;size=b4000_4000&amp;sec=1580814517&amp;di=2630beac440e5dab0e44c7286a3b2b61&amp;src=http://imgsrc.baidu.com/forum/w=580/sign=12c730c4ff03738dde4a0c2a831ab073/9497794f9258d1091818e6d6d858ccbf6d814d1b.jpg" alt="炸鸡"></p><h3 id="本地图片"><a href="#本地图片" class="headerlink" title="本地图片"></a>本地图片</h3><pre class="line-numbers language-markdown"><code class="language-markdown"><span class="token url">![friedChicken](friedChicken.jpg)</span>在同一个文件夹里（用相对路径）或者直接拷贝<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p><img src="/friedChicken.jpg" alt="friedChicken"></p><p><img src="/../cola.jpg" alt="cola"></p><h2 id="利用Markdown画图（需勾选扩展语法）"><a href="#利用Markdown画图（需勾选扩展语法）" class="headerlink" title="利用Markdown画图（需勾选扩展语法）"></a>利用Markdown画图（需勾选扩展语法）</h2><p><img src="/image-20200211211500416.png" alt="image-20200211211500416"></p><p>markdown画图也是轻量级的，功能并不全。</p><p>Mermaid 是一个用于画流程图、状态图、时序图、甘特图的库，使用 JS 进行本地渲染，广泛集成于许多 Markdown 编辑器中。Mermaid 作为一个使用 JS 渲染的库，生成的不是一个“图片”，而是一段 HTML 代码。</p><p>（不同的编辑器渲染的可能不一样）</p><h3 id="流程图-graph"><a href="#流程图-graph" class="headerlink" title="流程图(graph)"></a>流程图(graph)</h3><h4 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h4><pre class="line-numbers language-markdown"><code class="language-markdown">graph 方向描述<span class="token code keyword">    图表中的其他语句...</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><p>关键字graph表示一个流程图的开始，同时需要指定该图的方向。</p><p>其中“方向描述”为：</p><table><thead><tr><th align="left">用词</th><th align="left">含义</th></tr></thead><tbody><tr><td align="left">TB</td><td align="left">从上到下</td></tr><tr><td align="left">BT</td><td align="left">从下到上</td></tr><tr><td align="left">RL</td><td align="left">从右到左</td></tr><tr><td align="left">LR</td><td align="left">从左到右</td></tr></tbody></table><blockquote><p>T = TOP，B = BOTTOM，L = LEFT，R = RIGHT，D = DOWN</p></blockquote><p>最常用的布局方向是TB、LR。</p><pre class="line-numbers language-markdown"><code class="language-markdown">graph TB;  A-->B  B-->C  C-->A <span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-mermaid"><code class="language-mermaid">graph TB;  A-->B  B-->C  C-->A<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-markdown"><code class="language-markdown">graph LR;  A-->B  B-->C  C-->A<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-mermaid"><code class="language-mermaid">graph LR;  A-->B  B-->C  C-->A<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><h4 id="流程图常用符号及含义"><a href="#流程图常用符号及含义" class="headerlink" title="流程图常用符号及含义"></a>流程图常用符号及含义</h4><h5 id="节点形状"><a href="#节点形状" class="headerlink" title="节点形状"></a>节点形状</h5><table><thead><tr><th align="left">表述</th><th align="left">说明</th><th>含义</th></tr></thead><tbody><tr><td align="left">id[文字]</td><td align="left">矩形节点</td><td>表示过程，也就是整个流程中的一个环节</td></tr><tr><td align="left">id(文字)</td><td align="left">圆角矩形节点</td><td>表示开始和结束</td></tr><tr><td align="left">id((文字))</td><td align="left">圆形节点</td><td>表示连接。为避免流程过长或有交叉，可将流程切开。成对</td></tr><tr><td align="left">id{文字}</td><td align="left">菱形节点</td><td>表示判断、决策</td></tr><tr><td align="left">id&gt;文字]</td><td align="left">右向旗帜状节点</td><td></td></tr></tbody></table><p><strong>单向箭头线段</strong>：表示流程进行方向</p><blockquote><p>id即为节点的唯一标识，A~F 是当前节点名字，类似于变量名，画图时便于引用</p><p>括号内是节点中要显示的文字，默认节点的名字和显示的文字都为A</p></blockquote><pre class="line-numbers language-markdown"><code class="language-markdown">graph TB  A  B(圆角矩形节点)  C[矩形节点]  D((圆形节点))  E{菱形节点}  F>右向旗帜状节点] <span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-mermaid"><code class="language-mermaid">graph TB  A  B(圆角矩形节点)  C[矩形节点]  D((圆形节点))  E{菱形节点}  F>右向旗帜状节点]<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-markdown"><code class="language-markdown">graph TB<span class="token code keyword">    begin(出门)--> buy[买炸鸡]</span><span class="token code keyword">    buy --> IsRemaining{"还有没有炸鸡？"}</span><span class="token code keyword">    IsRemaining -->|有|happy[买完炸鸡开心]--> goBack(回家)</span><span class="token code keyword">    IsRemaining --没有--> sad["伤心"]--> goBack</span>    <span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-mermaid"><code class="language-mermaid">graph TB    begin(出门)--> buy[买炸鸡]    buy --> IsRemaining{"还有没有炸鸡？"}    IsRemaining -->|有|happy[买完炸鸡开心]--> goBack(回家)    IsRemaining --没有--> sad["伤心"]--> goBack    <span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h5 id="连线"><a href="#连线" class="headerlink" title="连线"></a>连线</h5><pre class="line-numbers language-markdown"><code class="language-markdown">graph TB  A1-->B1  A2---B2  A3--text---B3  A4--text-->B4  A5-.-B5  A6-.->B6  A7-.text.-B7  A8-.text.->B8  A9===B9  A10==>B10  A11==text===B11  A12==text==>B12<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-mermaid"><code class="language-mermaid">graph TB  A1-->B1  A2---B2  A3--text---B3  A4--text-->B4  A5-.-B5  A6-.->B6  A7-.text.-B7  A8-.text.->B8  A9===B9  A10==>B10  A11==text===B11  A12==text==>B12<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-mermaid"><code class="language-mermaid">graph TB A ---B<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span></span></code></pre><h5 id="子图表"><a href="#子图表" class="headerlink" title="子图表"></a>子图表</h5><p>使用以下语法添加子图表</p><pre class="line-numbers language-markdown"><code class="language-markdown">subgraph 子图表名称<span class="token code keyword">    子图表中的描述语句...</span>end<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-markdown"><code class="language-markdown">graph TB<span class="token code keyword">      subgraph 买炸鸡前</span><span class="token code keyword">                begin(出门)--> buy[出门买炸鸡]</span><span class="token code keyword">    end</span><span class="token code keyword">    buy --> IsRemaining{"还有没有炸鸡？"}</span><span class="token code keyword">    IsRemaining --没有--> sad["伤心"]--> goBack(回家)</span><span class="token code keyword">    IsRemaining -->|有|happy[买完炸鸡开心]--> goBack</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-mermaid"><code class="language-mermaid">graph TB      subgraph 买炸鸡前                begin(出门)--> buy[出门买炸鸡]    end    buy --> IsRemaining{"还有没有炸鸡？"}    IsRemaining --没有--> sad["伤心"]--> goBack(回家)    IsRemaining -->|有|happy[买完炸鸡开心]--> goBack<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="序列图-sequence-diagram"><a href="#序列图-sequence-diagram" class="headerlink" title="序列图(sequence diagram)"></a>序列图(sequence diagram)</h3><h4 id="概述-1"><a href="#概述-1" class="headerlink" title="概述"></a>概述</h4><pre class="line-numbers language-markdown"><code class="language-markdown">sequenceDiagram <span class="token code keyword">    [参与者1][消息线][参与者2]:消息体</span><span class="token code keyword">    ...</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><blockquote><p><code>sequenceDiagram</code> 为每幅时序图的固定开头</p></blockquote><pre class="line-numbers language-markdown"><code class="language-markdown">sequenceDiagram<span class="token code keyword">        Title: 买炸鸡</span><span class="token code keyword">    救救->>炸鸡店小哥: 还有炸鸡吗？</span><span class="token code keyword">    炸鸡店小哥-->>救救: 没有，要现炸</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-mermaid"><code class="language-mermaid">sequenceDiagram        Title: 买炸鸡    救救->>炸鸡店小哥: 还有炸鸡吗？    炸鸡店小哥-->>救救: 没有，要现炸<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span></span></code></pre><h4 id="参与者（participant）"><a href="#参与者（participant）" class="headerlink" title="参与者（participant）"></a>参与者（participant）</h4><p>传统时序图概念中参与者有角色和类对象之分，但这里我们不做此区分，用参与者表示一切参与交互的事物，可以是人、类对象、系统等形式。中间竖直的线段从上至下表示时间的流逝。</p><pre class="line-numbers language-markdown"><code class="language-markdown">sequenceDiagram<span class="token code keyword">    participant 参与者 1</span><span class="token code keyword">    participant 参与者 2</span><span class="token code keyword">    ...</span><span class="token code keyword">    participant 简称 as 参与者 3 #该语法可以在接下来的描述中使用简称来代替参与者 3</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p><code>participant &lt;参与者名称&gt;</code> 声明参与者，语句次序即为参与者横向排列次序。</p></blockquote><h4 id="消息线"><a href="#消息线" class="headerlink" title="消息线"></a>消息线</h4><table><thead><tr><th align="left">类型</th><th align="left">描述</th></tr></thead><tbody><tr><td align="left">-&gt;</td><td align="left">无箭头的实线</td></tr><tr><td align="left">–&gt;</td><td align="left">无箭头的虚线</td></tr><tr><td align="left">-&gt;&gt;</td><td align="left">有箭头的实线（主动发出消息）</td></tr><tr><td align="left">–-&gt;&gt;</td><td align="left">有箭头的虚线（响应）</td></tr><tr><td align="left">-x</td><td align="left">末端为叉的实线（表示异步）</td></tr><tr><td align="left">–x</td><td align="left">末端为叉的虚线（表示异步）</td></tr></tbody></table><h4 id="处理中-激活框"><a href="#处理中-激活框" class="headerlink" title="处理中-激活框"></a>处理中-激活框</h4><p>从消息接收方的时间线上标记一小段时间，表示对消息进行处理的时间间隔。</p><p>在消息线末尾增加 <code>+</code> ，则消息接收者进入当前消息的“处理中”状态；<br>在消息线末尾增加 <code>-</code> ，则消息接收者离开当前消息的“处理中”状态。</p><pre class="line-numbers language-markdown"><code class="language-markdown">sequenceDiagram<span class="token code keyword">    participant 99 as 救救</span><span class="token code keyword">    participant seller as 炸鸡店小哥</span><span class="token code keyword">    99 ->> seller: 还有炸鸡吗？</span><span class="token code keyword">    seller -->> 99: 没有，要现炸。</span><span class="token code keyword">    99 -x +seller:给我炸！</span><span class="token code keyword">    seller -->> -99: 您的炸鸡好了！</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-mermaid"><code class="language-mermaid">sequenceDiagram    participant 99 as 救救    participant seller as 炸鸡店小哥    99 ->> seller: 还有炸鸡吗？    seller -->> 99: 没有，要现炸。    99 -x +seller:给我炸！    seller -->> -99: 您的炸鸡好了！    <span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="注解（note）"><a href="#注解（note）" class="headerlink" title="注解（note）"></a>注解（note）</h4><p>语法如下</p><pre class="line-numbers language-markdown"><code class="language-markdown">Note 位置表述 参与者: 标注文字<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>其中位置表述可以为</p><table><thead><tr><th align="left">表述</th><th align="left">含义</th></tr></thead><tbody><tr><td align="left">right of</td><td align="left">右侧</td></tr><tr><td align="left">left of</td><td align="left">左侧</td></tr><tr><td align="left">over</td><td align="left">在当中，可以横跨多个参与者</td></tr></tbody></table><pre class="line-numbers language-markdown"><code class="language-markdown">sequenceDiagram<span class="token code keyword">    participant 99 as 救救</span><span class="token code keyword">    participant seller as 炸鸡店小哥</span><span class="token code keyword">    Note over 99,seller : 热爱炸鸡</span><span class="token code keyword">    Note left of 99 : 女</span><span class="token code keyword">    Note right of seller : 男</span><span class="token code keyword">    99 ->> seller: 还有炸鸡吗？</span><span class="token code keyword">    seller -->> 99: 没有，要现炸。</span><span class="token code keyword">    99 -x +seller : 给我炸！</span><span class="token code keyword">    seller -->> -99: 您的炸鸡好了！</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-mermaid"><code class="language-mermaid">sequenceDiagram    participant 99 as 救救    participant seller as 炸鸡店小哥    Note over 99,seller : 热爱炸鸡    Note left of 99 : 女    Note right of seller : 男    99 ->> seller: 还有炸鸡吗？    seller -->> 99: 没有，要现炸。    99 -x +seller : 给我炸！    seller -->> -99: 您的炸鸡好了！<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="循环（loop）"><a href="#循环（loop）" class="headerlink" title="循环（loop）"></a>循环（loop）</h4><p>在条件满足时，重复发出消息序列。（相当于编程语言中的 while 语句。）</p><pre class="line-numbers language-markdown"><code class="language-markdown">sequenceDiagram<span class="token code keyword">    participant 99 as 救救</span><span class="token code keyword">    participant seller as 炸鸡店小哥</span>   <span class="token code keyword">    99 ->> seller: 还有炸鸡吗？</span><span class="token code keyword">    seller -->> 99: 没有，要现炸。</span><span class="token code keyword">    99 ->> +seller:给我炸！</span><span class="token code keyword">    loop 三分钟一次</span><span class="token code keyword">        99 ->> seller : 我的炸鸡好了吗？</span><span class="token code keyword">        seller -->> 99 : 正在炸</span><span class="token code keyword">    end</span><span class="token code keyword">    seller -->> -99: 您的炸鸡好了！</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-mermaid"><code class="language-mermaid">sequenceDiagram    participant 99 as 救救    participant seller as 炸鸡店小哥       99 ->> seller: 还有炸鸡吗？    seller -->> 99: 没有，要现炸。    99 ->> +seller:给我炸！    loop 三分钟一次        99 ->> seller : 我的炸鸡好了吗？        seller -->> 99 : 正在炸    end    seller -->> -99: 您的炸鸡好了！<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="选择（alt）"><a href="#选择（alt）" class="headerlink" title="选择（alt）"></a>选择（alt）</h4><p>在多个条件中作出判断，每个条件将对应不同的消息序列。（相当于 if 及 else if 语句。）</p><pre class="line-numbers language-markdown"><code class="language-markdown">sequenceDiagram    <span class="token code keyword">    participant 99 as 救救</span><span class="token code keyword">    participant seller as 炸鸡店小哥</span><span class="token code keyword">    99 ->> seller : 现在就多少只炸好的炸鸡？</span><span class="token code keyword">    seller -->> 99 : 可卖的炸鸡数</span>    <span class="token code keyword">    alt 可卖的炸鸡数 > 3</span><span class="token code keyword">        99 ->> seller : 买三只！</span><span class="token code keyword">    else 1 &lt; 可卖的炸鸡数 &lt; 3</span><span class="token code keyword">        99 ->> seller : 有多少买多少</span><span class="token code keyword">    else 可卖的炸鸡数 &lt; 1</span><span class="token code keyword">        99 ->> seller : 那我明天再来</span><span class="token code keyword">    end</span><span class="token code keyword">    seller -->> 99 : 欢迎下次光临</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-mermaid"><code class="language-mermaid">sequenceDiagram        participant 99 as 救救    participant seller as 炸鸡店小哥    99 ->> seller : 现在就多少只炸好的炸鸡？    seller -->> 99 : 可卖的炸鸡数        alt 可卖的炸鸡数 > 3        99 ->> seller : 买三只！    else 1 < 可卖的炸鸡数 < 3        99 ->> seller : 有多少买多少    else 可卖的炸鸡数 < 1        99 ->> seller : 那我明天再来    end    seller -->> 99 : 欢迎下次光临<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="可选（opt）"><a href="#可选（opt）" class="headerlink" title="可选（opt）"></a>可选（opt）</h4><p>在某条件满足时执行消息序列，否则不执行。相当于单个分支的 if 语句。</p><pre class="line-numbers language-markdown"><code class="language-markdown">sequenceDiagram<span class="token code keyword">    participant 99 as 救救</span><span class="token code keyword">    participant seller as 炸鸡店小哥</span><span class="token code keyword">    99 ->> seller : 买炸鸡</span><span class="token code keyword">    opt 全都卖完了</span><span class="token code keyword">        seller -->> 99 : 下次再来</span><span class="token code keyword">    end</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-mermaid"><code class="language-mermaid">sequenceDiagram    participant 99 as 救救    participant seller as 炸鸡店小哥    99 ->> seller : 买炸鸡    opt 全都卖完了        seller -->> 99 : 下次再来    end<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h4 id="并行（Par）"><a href="#并行（Par）" class="headerlink" title="并行（Par）"></a>并行（Par）</h4><p>将消息序列分成多个片段，这些片段并行执行。</p><pre class="line-numbers language-markdown"><code class="language-markdown">sequenceDiagram   participant 99 as 救救   participant seller as 炸鸡店小哥   <span class="token code keyword">    99 ->> seller : 一个炸鸡，一杯可乐！</span><span class="token code keyword">    par 并行执行</span><span class="token code keyword">        seller ->> seller : 装可乐</span><span class="token code keyword">    and</span><span class="token code keyword">        seller ->> seller : 炸炸鸡</span><span class="token code keyword">    end</span><span class="token code keyword">    seller -->> 99 : 您的炸鸡好了！</span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-mermaid"><code class="language-mermaid">sequenceDiagram   participant 99 as 救救   participant seller as 炸鸡店小哥       99 ->> seller : 一个炸鸡，一杯可乐！    par 并行执行        seller ->> seller : 装可乐    and        seller ->> seller : 炸炸鸡    end    seller -->> 99 : 您的炸鸡好了！<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h3 id="饼图（Pie）"><a href="#饼图（Pie）" class="headerlink" title="饼图（Pie）"></a>饼图（Pie）</h3><pre class="line-numbers language-markdown"><code class="language-markdown">pie<span class="token code keyword">    title Pie Chart</span><span class="token code keyword">    "Dogs" : 386</span><span class="token code keyword">    "Cats" : 85</span><span class="token code keyword">    "Rats" : 150 </span><span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span></span></code></pre><pre class="line-numbers language-mermaid"><code class="language-mermaid">pie    title Pie Chart    "Dogs" : 386    "Cats" : 85    "Rats" : 150     "panda" : 200<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p><a href="http://support.typora.io/Draw-Diagrams-With-Markdown/">Typora支持mermaid的官方链接</a></p></blockquote><h3 id="甘特图（gantt）"><a href="#甘特图（gantt）" class="headerlink" title="甘特图（gantt）"></a>甘特图（gantt）</h3><pre class="line-numbers language-markdown"><code class="language-markdown">  title 标题<span class="token code keyword">    dateFormat 日期格式</span><span class="token code keyword">    section 部分名</span><span class="token code keyword">    任务名:参数一, 参数二, 参数三, 参数四，参数五</span>   //参数一：crit（是否重要，红框框） 或者 不填  //参数二：done（已完成）、active（正在进行） 或者 不填(表示为待完成状态)  //参数三：取小名 或者 不填  //参数四：任务开始时间  //参数五：任务结束时间<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><blockquote><p><a href="https://mermaid-js.github.io/mermaid/#/gantt">官方教程</a></p></blockquote><pre><code>gantt       dateFormat  YYYY-MM-DD       title Adding GANTT diagram functionality to mermaid       section A section       Completed task            :done,    des1, 2014-01-06,2014-01-08       Active task               :active,  des2, 2014-01-09, 3d       Future task               :         des3, after des2, 5d       Future task2              :         des4, after des3, 5d       section Critical tasks       Completed task in the critical line :crit, done, 2014-01-06,24h       Implement parser and jison          :crit, done, after des1, 2d       Create tests for parser             :crit, active, 3d       Future task in critical line        :crit, 5d       Create tests for renderer           :2d       Add to mermaid                      :1d       section Documentation       Describe gantt syntax               :active, a1, after des1, 3d       Add gantt diagram to demo page      :after a1  , 20h       Add another diagram to demo page    :doc1, after a1  , 48h       section Last section       Describe gantt syntax               :after doc1, 3d       Add gantt diagram to demo page      :20h       Add another diagram to demo page    :48h</code></pre><pre class="line-numbers language-mermaid"><code class="language-mermaid">gantt       dateFormat  YYYY-MM-DD       title Adding GANTT diagram functionality to mermaid       section A section       Completed task            :done,    des1, 2014-01-06,2014-01-08       Active task               :active,  des2, 2014-01-09, 3d       Future task               :         des3, after des2, 5d       Future task2              :         des4, after des3, 5d       section Critical tasks       Completed task in the critical line :crit, done, 2014-01-06,24h       Implement parser and jison          :crit, done, after des1, 2d       Create tests for parser             :crit, active, 3d       Future task in critical line        :crit, 5d       Create tests for renderer           :2d       Add to mermaid                      :1d       section Documentation       Describe gantt syntax               :active, a1, after des1, 3d       Add gantt diagram to demo page      :after a1  , 20h       Add another diagram to demo page    :doc1, after a1  , 48h       section Last section       Describe gantt syntax               :after doc1, 3d       Add gantt diagram to demo page      :20h       Add another diagram to demo page    :48h<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="Typora快捷方式（系统默认）"><a href="#Typora快捷方式（系统默认）" class="headerlink" title="Typora快捷方式（系统默认）"></a>Typora快捷方式（系统默认）</h2><h3 id="文件"><a href="#文件" class="headerlink" title="文件"></a>文件</h3><table><thead><tr><th align="left">功能</th><th align="left">热键 (Windows/Linux)</th><th>热键 (macOS)</th></tr></thead><tbody><tr><td align="left">新建</td><td align="left">Ctrl + N</td><td>Command  + N</td></tr><tr><td align="left">新窗口</td><td align="left">Ctrl + Shift + N</td><td>Command  +Shift + N</td></tr><tr><td align="left">新标签</td><td align="left"><em>（不支持）</em></td><td>Command  + T</td></tr><tr><td align="left">打开</td><td align="left">Ctrl + O</td><td>Command  + O</td></tr><tr><td align="left">快速打开</td><td align="left">Ctrl + P</td><td>Command  + Shift + O</td></tr><tr><td align="left">重新打开关闭的文件</td><td align="left">Ctrl + Shift + T</td><td>Command  + Shift + T</td></tr><tr><td align="left">保存</td><td align="left">Ctrl + S</td><td>Command  + S</td></tr><tr><td align="left">另存为/复制</td><td align="left">Ctrl + Shift + S</td><td>Command + Shift + S</td></tr><tr><td align="left">关闭</td><td align="left">Ctrl + W</td><td>Command  + W</td></tr></tbody></table><h3 id="编辑"><a href="#编辑" class="headerlink" title="编辑"></a>编辑</h3><table><thead><tr><th align="left">功能</th><th align="left">热键 (Windows/Linux)</th><th>热键 (macOS)</th></tr></thead><tbody><tr><td align="left">新段落</td><td align="left">Enter</td><td>Enter</td></tr><tr><td align="left">新行</td><td align="left">Shift + Enter</td><td>Shift + Enter</td></tr><tr><td align="left">剪切</td><td align="left">Ctrl + X</td><td>Command  + X</td></tr><tr><td align="left">复制</td><td align="left">Ctrl + C</td><td>Command  + C</td></tr><tr><td align="left">粘贴</td><td align="left">Ctrl + V</td><td>Command  + V</td></tr><tr><td align="left">复制为 Markdown</td><td align="left">Ctrl + Shift + C</td><td>Command + Shift + C</td></tr><tr><td align="left">粘贴为纯文本</td><td align="left">Ctrl + Shift + V</td><td>Command + Shift + V</td></tr><tr><td align="left">全选</td><td align="left">Ctrl + A</td><td>Command + A</td></tr><tr><td align="left">选择行/句子 选择行（在表格中）</td><td align="left">Ctrl + L</td><td>Command + L</td></tr><tr><td align="left">删除行（在表中）</td><td align="left">Ctrl + Shift + Backspace</td><td>Command + Shift + Backspace</td></tr><tr><td align="left">选择样式范围 选择单元格（在表格中）</td><td align="left">Ctrl + E</td><td>Command + E</td></tr><tr><td align="left">选择单词</td><td align="left">Ctrl + D</td><td>Command + D</td></tr><tr><td align="left">删除单词</td><td align="left">Ctrl + Shift + D</td><td>Command + Shift + D</td></tr><tr><td align="left">跳到顶部</td><td align="left">Ctrl + home</td><td>Command + ↑</td></tr><tr><td align="left">跳转到选择</td><td align="left">Ctrl + J</td><td>Command + J</td></tr><tr><td align="left">跳到底部</td><td align="left">Ctrl + end</td><td>Command + ↓</td></tr><tr><td align="left">寻找</td><td align="left">Ctrl + F</td><td>Command + F</td></tr><tr><td align="left">找下一个</td><td align="left">F3 / Enter</td><td>Command + G / Enter</td></tr><tr><td align="left">查找上一个</td><td align="left">Shift + F3 / Shift + Enter</td><td>Command + Shift + G / Shift + Enter</td></tr><tr><td align="left">代替</td><td align="left">Ctrl + H</td><td>Command + H</td></tr></tbody></table><h3 id="段落"><a href="#段落" class="headerlink" title="段落"></a>段落</h3><table><thead><tr><th align="left">功能</th><th align="left">热键 (Windows/Linux)</th><th>热键 (macOS)</th></tr></thead><tbody><tr><td align="left">标题 1 至 6</td><td align="left">Ctrl + 1/2/3/4/5/6</td><td>Command + 1/2/3/4/5/6</td></tr><tr><td align="left">增加标题级别</td><td align="left">Ctrl + =</td><td>Command + =</td></tr><tr><td align="left">降低标题级别</td><td align="left">Ctrl + -</td><td>Command  + -</td></tr><tr><td align="left">桌子</td><td align="left">Ctrl + T</td><td>Command + Option + T</td></tr><tr><td align="left">代码围栏</td><td align="left">Ctrl + Shift + K</td><td>Command  + Option + C</td></tr><tr><td align="left">数学块</td><td align="left">Ctrl + Shift + M</td><td>Command + Option + B</td></tr><tr><td align="left">引用</td><td align="left">Ctrl + Shift + Q</td><td>Command  + Option + Q</td></tr><tr><td align="left">有序列表</td><td align="left">Ctrl + Shift + [</td><td>Command + Option + O</td></tr><tr><td align="left">无序列表</td><td align="left">Ctrl + Shift + ]</td><td>Command + Option + U</td></tr><tr><td align="left">缩进一个制表符</td><td align="left">Ctrl + [ / Tab</td><td>Command + [ / Tab</td></tr><tr><td align="left">前进一个制表符</td><td align="left">Ctrl + ] / Shift + Tab</td><td>Command + ] / Shift + Tab</td></tr></tbody></table><h3 id="格式"><a href="#格式" class="headerlink" title="格式"></a>格式</h3><table><thead><tr><th align="left">功能</th><th align="left">热键 (Windows/Linux)</th><th>热键 (macOS)</th></tr></thead><tbody><tr><td align="left">字体加粗</td><td align="left">Ctrl + B</td><td>Command + B</td></tr><tr><td align="left">斜体</td><td align="left">Ctrl + I</td><td>Command +I</td></tr><tr><td align="left">下划线</td><td align="left">Ctrl + U</td><td>Command + U</td></tr><tr><td align="left">代码</td><td align="left">Ctrl + Shift + `</td><td>Command + Shift + `</td></tr><tr><td align="left">删除线</td><td align="left">Alt + Shift + 5</td><td>Control+ Shift + `</td></tr><tr><td align="left">超链接</td><td align="left">Ctrl + K</td><td>Command  + K</td></tr><tr><td align="left">图片</td><td align="left">Ctrl + Shift + I</td><td>Command +Control+I</td></tr><tr><td align="left">清除格式</td><td align="left">Ctrl + \</td><td>Command + \</td></tr></tbody></table><h3 id="view"><a href="#view" class="headerlink" title="view"></a>view</h3><table><thead><tr><th align="left">功能</th><th align="left">热键 (Windows/Linux)</th><th>热键 (macOS)</th></tr></thead><tbody><tr><td align="left">切换侧边栏</td><td align="left">Ctrl + Shift + L</td><td>Command + Shift + L</td></tr><tr><td align="left">显示大纲</td><td align="left">Ctrl + Shift + 1</td><td>Command + 控制 + 1</td></tr><tr><td align="left">显示文件</td><td align="left">Ctrl + Shift + 2</td><td>Command + 控制 + 2</td></tr><tr><td align="left">显示文件树</td><td align="left">Ctrl + Shift + 3</td><td>Command + 控制 + 3</td></tr><tr><td align="left">源代码模式(再次取消)</td><td align="left">Ctrl + /</td><td>Command + /</td></tr><tr><td align="left">对焦模式</td><td align="left">F8</td><td>F8</td></tr><tr><td align="left">打字机模式</td><td align="left">F9</td><td>F9</td></tr><tr><td align="left">切换全屏</td><td align="left">F11</td><td>Command + Option + F</td></tr><tr><td align="left">真实大小</td><td align="left">Ctrl + Shift + 0</td><td><em>（不支持）</em></td></tr><tr><td align="left">放大</td><td align="left">Ctrl + Shift + =</td><td><em>（不支持）</em></td></tr><tr><td align="left">缩小</td><td align="left">Ctrl + Shift + -</td><td><em>（不支持）</em></td></tr><tr><td align="left">在打开的文档之间切换</td><td align="left">Ctrl + Tab</td><td>命令 + `</td></tr></tbody></table><h2 id="Typora自定义快捷方式"><a href="#Typora自定义快捷方式" class="headerlink" title="Typora自定义快捷方式"></a>Typora自定义快捷方式</h2><p>Typora有些功能是没有快捷键的，需要使用者自定义。这里以给<code>内联公式</code>设置快捷键为例，讲解如何自定义快捷键。</p><h3 id="给内联公式设置自定义快捷方式"><a href="#给内联公式设置自定义快捷方式" class="headerlink" title="给内联公式设置自定义快捷方式"></a>给内联公式设置自定义快捷方式</h3><ol><li>在进行设置前，推荐将软件的语言设置称英语，理由后面再说。具体操作:<br>文 件 ⟹ 偏 好 设 置 ⟹ 通 用 ⟹ 语 言 ， 选 择 ‘ English ’ ， 重 新 启 动 。</li><li>重新启动完成后，查看二级菜单中<code>内联公式</code>的英文名称是<code>Inline Math</code>，记下该拼写。<img src="https://img-blog.csdnimg.cn/20210626224751567.png#pic_center" alt="img"></li><li>再次打开<code>偏好设置</code>（快捷键<code>Ctrl+,</code>），在<code>通用</code>中找到<code>高级设置</code>，具体操作：File ⟹ Preferences ⟹ General ⟹ Open Advanced Setting</li></ol><p><img src="https://img-blog.csdnimg.cn/20210626225203912.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L1FDU1lTWlE=,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述"></p><ol start="4"><li><p>之后会显示一个文件夹，有两个配置文件，选择<code>conf.user.json</code>（在另一个文件中设置无效），打开之后找到<code>keyBinding</code>，在下面添加<code>"Inline Math":"Ctrl+M"</code>（这里的快捷键我设置成了<code>Ctrl +M</code>，在保证快捷键不冲突的条件下，可以随意设置），如图所示</p><p><img src="https://img-blog.csdnimg.cn/20210626225840503.png#pic_center" alt="在这里插入图片描述"></p></li></ol><p>保存配置文件，重启Typora，可以发现在选项<code>Inline Math</code>旁边显示了快捷键<code>Ctrl +M</code>，经过测试快捷键能正常使用。</p><h3 id="为什么不在中文环境下配置？"><a href="#为什么不在中文环境下配置？" class="headerlink" title="为什么不在中文环境下配置？"></a>为什么不在中文环境下配置？</h3><p>在中文环境下，也可以进行设置，在配置文件中添加<code>"内联公式":"Ctrl+M"</code>，也可以换成<code>"Inline Math":"Ctrl+M"</code>，在不知道菜单选项的英文名称时可以直接写上对应的中文名称（所以推荐在开始设置前将Typora设置成英文环境，便于查看菜单项的英文名称），如</p><p><img src="https://img-blog.csdnimg.cn/20210626231115945.png#pic_center" alt="在这里插入图片描述"></p><p>重启Typora</p><p><img src="https://img-blog.csdnimg.cn/20210626231529681.png#pic_center" alt="在这里插入图片描述"></p><p>中文菜单<code>内联公式</code>旁出现了设置的快捷键，表示快捷键设置成功。<br>但是我如果心血来潮，想切换成英文环境，再查看菜单项的快捷键时发现快捷键没有了</p><p><img src="https://img-blog.csdnimg.cn/20210626231943998.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L1FDU1lTWlE=,size_16,color_FFFFFF,t_70#pic_center" alt="在这里插入图片描述"></p><p>如果当初我在配置文件中使用<code>"Inline Math":"Ctrl+M"</code>，不使用中文的名称，无论我将Typora的语言环境设置成何种语言，都不会影响自定义快捷键的使用。</p>]]></content>
      
      
      <categories>
          
          <category> MarkDown </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Typora </tag>
            
            <tag> Markdown </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
